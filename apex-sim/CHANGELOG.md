# 변경 이력

## 배치 C — 충돌 반복·고속 안정·현실적인 섀시 (사용자 요청: 크래시 반복, 고속 떨림, 바퀴 부풀기, 서스펜션·핸들링, 나머지 차)

### 수정 — 크래시 랩
- **첫 충돌 뒤 다른 충돌을 못 하던 문제(휴대폰)**: 패널의 탭 막대가 좁은 화면에서 20 px로 눌려(flex 축소) 탭이 사실상 안 보였고, 충돌 결과가 뜨면 패널이 '리포트' 탭으로 넘어가 프리셋으로 돌아갈 길이 없었다. 결과 카드는 발사 버튼과 프리셋 타일을 덮었다. → 탭 막대 고정 높이, 자동 탭 전환 제거, 결과 카드에 **다른 시나리오**·**리포트** 버튼, 휴대폰에서는 작은 카드가 발사 버튼 위에 뜨고 패널을 열면 카드가 비킨다. 패널이 열리면 발사 버튼은 패널 위로 올라간다
- **손상 유지하고 이어서**(새 기능): 켜면 다음 테스트에 지금 부서진 차를 그대로 쓴다 — 코어 `World::relaunchVehicle`이 차체 전체를 강체로 옮겨(빔·소성·공기압 상태 그대로) 출발선에 똑바로 세우고 발사 속도·바퀴 회전을 준다. 떨어져 나간 부품은 치우고, 충돌 센서·접지 앵커·슬립 상태만 새로 시작한다(에너지는 외부 일로 장부에 기록). 이벤트 로그는 이전 판의 사건을 다시 보이지 않는다. 정면 → 기둥 → 다시 정면처럼 같은 차를 계속 부술 수 있다(테스트 `[relaunch]`: 두 번째 충돌이 별도 사건으로 기록되고 소성 일이 더 쌓이며 수지가 맞음)
- **덜 튀는 충돌**: 격자 차체는 명시 적분 한계 때문에 실제 강판보다 훨씬 무르고, 세게 눌리면 탄성 에너지를 많이 품었다가 반발로 돌려줬다(고스트 56 km/h 벽: 반발 16.8 km/h). 코어 빔 속성 `unloadRatio`(α): 항복 하중의 80 % 이상 눌린(또는 항복한) 부재는 되돌아올 때 α배 가파른 기울기로 풀린다(휴지 길이가 되돌아오는 길이를 따라감 — 찌그러진 강판은 찌그러진 채 남는다). 되돌려주지 않은 탄성 에너지는 소성 일로 기록된다. **크럼플 존(앞·뒤)에만** α = 5, 하드포인트·서브프레임도 같다. 승객 셀은 제외해 충돌 뒤에도 제 모양으로 돌아온다(셀 전체에 걸었더니 64 km/h에서 셀이 20 cm 줄어든 채 남아 뺐다). 56 km/h 벽 반발: **고스트 16.8 → 11.7 km/h, 마이바흐 17.8 → 15.0 km/h, 911 64 km/h 15.0 → 13.4 km/h**(911 56 km/h는 9.3 → 10.8로 차이 없음), 소성 흡수 에너지 증가(고스트 167 → 190 kJ)
- **충돌 뒤 바퀴가 커졌다 작아졌다 하던 문제**: 충돌로 밀린 트레드 노드가 림 안쪽으로 빠져나가 공기가 찬 채 뒤집혀 걸려 있었다(찌그러진 모양이 바퀴와 함께 돌며 부풀었다 줄었다). 코어 타이어 빌더가 트레드 노드마다 허브까지 **림 배럴 범프스톱**(bounded 빔)을 달아 트레드는 림에 눌릴 뿐 통과하지 못한다. 림에 눌리면 핀치·파열 판정(하중 받은 노드뿐 아니라 모든 트레드 노드)
- 크래시 랩 덤프트럭 압착을 손상 유지로 다시 하면 이전 트럭을 치우고 시각을 이어서 쓴다
- 코어 섬 분리: 압축만 받는 요소(서포트 빔, 위 한계 없는 bounded 스톱 — 림 배럴)는 부품을 붙잡지 않는다(떨어진 바퀴·찢어진 트레드가 그대로 분리)
- 911 앞 후드: 래치가 풀린 채 280 km/h면 첫 충돌에 경첩 하나가 찢어지고 나머지 하나에 매달려 펄럭인다(앞 다운포스가 제대로 들어가 코가 눌린 뒤. 전에는 둘 다 찢어져 날아갔다). 시험을 '경첩이 찢어진다'로 조정
- 골든 해시 재생성(golden_m0, crash_pair): 상태 해시에 제하 상태·빠져 있던 에어백 값 추가, 림 배럴·후프 장력·공력 보정 변경

### 수정 — 고속 안정·서스펜션·핸들링
- **회전하는 바퀴의 원심 하중이 허브를 조이던 문제(모든 차)**: 림·트레드 노드의 원심력을 스포크·사이드월이 받아 회전하지 않는 허브 노드 둘을 축 방향으로 끌어당겼다(ω²에 비례, 200 km/h에서 8 kN). 911은 견뎠지만 고스트·마이바흐의 업라이트는 이 압축에 휘어 고속에서 토 아웃 5~11°, 떨림 1~2 g, 스핀. → 코어 `Vehicle::hoopTension`: 바퀴의 네 고리(트레드 두 줄, 림 두 줄)가 스스로 원심 하중을 지는 후프 장력 T = m′v²(실제 강철 림·벨트처럼). 둥근 고리에서는 각 노드의 구심력과 정확히 같고, 접지면에서 펴진 벨트는 안쪽으로 당기지 않는다. 내부 짝힘이라 운동량 보존. 허브 압축 8.4 kN → 0.4 kN, 911의 200 km/h 토 변화 +0.5° → 0.0°
- **911 고속 앞 들림**: 911 생성기의 부호 실수 — 경첩 패널(후드·트렁크)의 흡입 양력을 잔여 양력에서 빼야 할 것을 더해 앞 양력이 두 배가 됐다(250 km/h 앞바퀴 하중 3.0 → 1.9 kN, 300 km/h에서 떨림). 고친 뒤 250 km/h 앞 다운포스 1.13 kN, 뒤 0.41 kN
- **공력 보정**: 각 힘의 축 분담을 지렛대로 계산(뒤 차축 뒤의 윙은 앞을 들어 올림, 0~1로 자르지 않음)하고, 잔여 양력 두 개를 그 노드 위치의 지렛대까지 넣어 풀어 두 축이 정확히 목표 양력이 되게 함
- **고스트·마이바흐 앞 서스펜션**: 업라이트에 앞쪽 캘리퍼 마운트 노드를 더해 3차원 트러스로(모든 노드가 거의 한 평면에 있어 앞뒤 힘에 비틀려 토가 1.5° 변했다), 업라이트 감쇠 ζ 0.2, 링크는 유압 부싱처럼 ζ 0.35(타이로드·토링크 0.2), A암을 앞뒤로 벌림(0.42 / 0.52 m), 큰 타이어의 이완 길이(종 0.18 m, 횡 0.45 m). **결과: 고스트 150 → 283 km/h 가속 내내 떨림 0.01 g 미만, 토 0.5° 안팎으로 일정. 마이바흐 100·200 km/h 순항 떨림 0.003 g(전에는 0.7 g)**
- 코어: 차량 상태 해시에서 주석에 먹혀 빠져 있던 에어백·충돌 센서 값 4개를 다시 넣음

## 배치 B — 맵·UI (§13, §15.4, §18)

### 추가 — 배치 B 2차 (사용자 요청: 멋진 UI·3D 선택창·전체 3D 지도·사실적인 도시)
- **3D 맵 선택** — 메인 메뉴의 차 대신 맵의 미니어처(약 1:1000)가 무대에 올라온다: 지형 위에 도로 지도를 그린 판, 건물·물·숲·교량/고가, 출발 위치 핀. 맵 탭, 출발 위치 목록(핀을 눌러도 선택), 시각·날씨를 고르면 미니어처의 빛·비·눈·안개가 바로 바뀐다. 세로 휴대폰은 아래 시트, 가로 휴대폰은 옆 패널, 키보드(← → ↑ ↓ Enter Esc)
- **3D 전체 지도(자유 주행)** — 상단 바 버튼, 월드맵의 3D 버튼, O 키: 카메라가 차 뒤에서 떠올라 실제 월드 전체를 내려다본다(안개를 걷고 모든 타일을 그림). 회전·이동·확대, 내 차·목적지·선택 지점 빔, 경로 리본, 지명 라벨. 땅을 누르면 '여기로 이동'·'목적지로 설정'. 열려 있는 동안 물리는 멈춘다
- **계기판** — 속도 숫자 + 회전수 호 하나(레드존, 변속 경고 깜빡임), 고장 램프는 고장이 있을 때만
- **터치 조작(CRASH LAB 방식)** — ◀ ▶ 패드, 유리 질감의 가속·브레이크 페달(아이콘), 핸드브레이크·변속 버튼, 카메라·리셋 원형 버튼. 세로 모드: 계기판은 오른쪽 위(미니맵 맞은편), 시트는 버튼 뒤로
- **메인 메뉴** — 스튜디오 무대(배경 헤이즈, 반사 바닥, 림 조명), 시작 카메라 스윕, 차 교체 애니메이션, 제원 카운트업·막대
- **차 반사** — 하늘(해·구름·흐림)을 미리 필터링한 반사 맵을 차 재질만 쓴다(15분·날씨 단계마다 갱신). 유리를 어둡게 해 빈 실내가 회색 판으로 보이던 문제 해결
- **블룸** — 고품질에서 램프·창·해가 번진다(`?bloom=0`으로 끔)
- **크래시 랩** — 입장·시나리오 선택 시 차가 출발선에 서서 기다린다. 시나리오 타일에 도식 그림. 충돌 후 결과 카드(충돌 속도·최대 감속·최대 충격력·흡수 에너지·손상된 바퀴, 다시 발사)
- **도시** — 건물을 여러 상자로: 타워는 상가 포디움 + 몸체 + 셋백 + 옥상 설비(초고층은 안테나), 아파트는 옥상 승강기·계단실, 주택은 박공지붕, 창고는 낮은 별동. 아파트 발코니 띠, 밤에 타워 꼭대기 조명·상가 불빛, 멀리서는 창 무늬 대비를 줄여 모아레 제거
- **누리지하차도** — 중앙대로 밑을 지나는 지하차도(개착 경사로 + 92 m 박스, 터널 조명). 터널 입구 구멍이 입구 면 셀까지 열리도록 고침(평지의 짧은 지하차도에서 입구가 막혀 있던 문제)
- **아파트 단지 진입로와 방지턱** — 모든 대단지 블록에 가운데 진입로(T자 합류)와 노란·검정 줄무늬 원형 방지턱(3.6 m × 8 cm, 차로 면을 따라 턱 없이 시작), '과속방지턱' 표지. 맵 선택에 출발 위치 '누리지하차도', '아파트 단지 (방지턱)' 추가

### 추가 — 맵과 월드 (§13)
- **코어: 지형 높이장(heightfield)** — 규칙 격자 높이(x 우선) + 셀마다 재질(255 = 구멍, 터널 입구). 접촉 해석기가 바디 주변 셀의 삼각형만 그때그때 만들어(셀당 2개, (x0,z0)–(x1,z1) 대각선) 수백만 셀 지형도 높이 배열만큼의 메모리로 충돌한다. 같은 삼각형을 정적 메시로 넣은 것과 결과가 같다(테스트 `[heightfield]` 4개: 메시 동등성 2 mm 이내, 구멍 셀 통과, 포르쉐 3 % 오르막 주행, 날씨 재질 치환)
- **코어: 재질 치환(날씨)** `setMaterialRemap(from, to)` — 정적 삼각형·높이장·평면·데칼의 재질을 통째로 바꿈(비: 아스팔트→젖은 아스팔트, 잔디→젖은 잔디, 흙→진흙 …, 눈 쌓임: 압설·신설). C API `sbc_world_add_static_mesh`, `sbc_world_set_heightfield`, `sbc_world_set_material_remap`, 빈 장면 `"map"`(−500 m 안전 평면)
- **맵 생성기** (`web/src/world/builder.ts`, `road.ts`, `terrain.ts`, §13.4 파이프라인): 스플라인 중심선 → 곡률 적응 스테이션(현 오차 약 1 cm) → 종단 선형(평활 + 등급별 최대 경사 + 고정 높이·입체 교차 여유) → 곡률로 편경사(런오프 평활, 직선은 2 % 노면 경사) → 단면(차로 수·폭 3.0~3.6 m, 중앙분리대, 갓길, 연석·보도, 가드레일·방호벽, 길섶) → 절토·성토(경사 1:1.5, 도로 아래 고정, 비탈 평활) → 도로 삼각형 아래 지형 낮춤(셀 대각선 안 모든 표본을 삼각형 평면 8 cm 아래로: 지형이 도로를 뚫고 올라오지 않음) → 교량(지면 위 4.5 m 초과·수면 위, 교각·거더·파일론·사장 케이블) · 터널(입구 절취 14 m까지 자동 연장, 벽·천장·조명, 입구 구멍·포털) → 교차로(격자 도심 평면 교차의 포장 박스·신호등, T자 합류는 부모 도로 면으로 혼합·보도 개구) → 병렬 합류 램프 → 물리 타일(512 m, 재질별) · 렌더 타일 · 소품(가로등, 가로수, 가드레일 기둥, 표지판) · 경로 그래프
- **한빛시 (오픈월드, 36 km², 가상 지명)**: 강 북쪽 대도시(간선·이면도로 격자, 업무지구 초고층, 대단지 아파트, 급경사 구도심 언덕길·골목), 강변 고속도로(2+2, 중앙분리대, 교량 위 입체 교차), 다이아몬드 IC(램프 4개), 한강 모티프 강과 교량 2개(한빛대교 사장교), 북악터널(도심→북부 계곡), 한빛재 고갯길(헤어핀), 은빛호, 고속도로 한빛산 터널(약 1.5 km)과 요금소, 솔내읍 회전교차로·농로·비포장 숲길·채석장, 주유소, 강변 주차장(주차 차량 6대), 표지판(가상 지명), 숲 약 1.9만 그루
- **종합 주행시험장 (25 km²)**: 둘레 10.02 km 뱅크 고속 주회로(편경사 최대 0.32, 3차로), 지름 320 m 다이나믹 패드 + R 30/40 m 스키드패드 원, 병렬 제동 시험로 7차선(건조·젖은 아스팔트·저µ 타일·**µ-split**·자갈·압설·빙판), 핸들링 서킷(약 3.4 km, 고저차, 자갈 트랩), 경사로 5·10·20·30·40·60 %와 횡경사 20 %, 방지턱·승차감 시험로(원형·사다리꼴 험프, 럼블 스트립, 와시보드, 단차, 포트홀, 대형 험프, 코블, 철도 건널목), 오프로드(모굴, 바위 정원, 통나무, V자 도랑, 진흙, 급경사 언덕), 풍동·K&C 건물
- **맵 렌더러** (`MapView.ts`): 높이장 LOD 청크(128셀, 스텝 1~16, 스커트), 도로 셰이더(차선·중앙선·가장자리선·버스·횡단보도·정지선을 속성에서 그림, 젖으면 어둡고 반들, 밤에는 가로등 조명 웅덩이), 건물(인스턴스, 층·창 격자, 밤에 창 점등), 나무 3종(바람에 흔들림), 가로등·가드레일·신호등(주기 동작)·표지판(캔버스 텍스트)·교각·수면·원경 산맥, 품질별 가시 거리
- **환경** (`Environment.ts`): 24시간(위도·날짜로 해 위치, 달·별), 날씨 7종(맑음·흐림·비·폭우·안개·눈·눈보라)과 자연스러운 전환, 안개, 비·눈 입자(GPU), 흐린 하늘 돔, 젖음·적설 누적(노면 셰이더와 물리 재질), 바람(나무·물리), 헤드라이트
- **지도 UI** (`MapUI.ts`, `Route.ts`): 음영 기복도 + 도로 등급별 선 + 장소, 미니맵(회전·고정, 속도에 따라 축척), 월드맵(드래그·휠·핀치, 장소 목록, 텔레포트, 웨이포인트), A* 경로 안내(다음 회전·거리, 경로 이탈 시 재탐색, 도착)
- **자유 주행 모드**: 맵 생성 워커 + 로딩 화면(진행률·팁·셰이더 컴파일), 장소·시간·날씨 시트, 떨어지면 재시작, `?freeroam=<차>&map=hanbit|proving&spawn=&hour=&weather=`(아티팩트: `#freeroam.<차>.<맵>.<장소>`)

### 추가 — UI (§18, §15.4)
- 공통 셸: 상단 바(메뉴·제목·아이콘 액션) + 모드별 시트 하나(데스크톱 오른쪽 패널, 휴대폰 세로 하단 시트, 가로 휴대폰은 상단 버튼으로 여닫음) + 일시정지 메뉴 + 설정(게임플레이·조작·그래픽·접근성, 기본값 복원)
- 메인 메뉴: 쇼룸 턴테이블(선택 차 제원), 모드 카드(자유 주행·테스트 그라운드·크래시 랩·샌드박스·차고) + 이어하기, **맵 선택**(§18.3-6: 맵·출발 위치·시각·날씨)
- 터치 조작(§15.4): 조향 4방식(버튼·휠·슬라이더·기울기), 압력 반응 페달, 자동 가속, 햅틱, 크기·투명도, 배치 편집
- HUD 프리셋(없음·미니멀·레이싱·엔지니어), km/h·mph, G 미터, 휴대폰에서는 작은 대시
- 크래시 랩: 시나리오 타일(정면·오프셋·스몰 오버랩·정면 차대차·측면·기둥·후방·전복·낙하·고속) → 세부 설정 → 발사 버튼 → 리포트(로그·휠 표·그래프)
- 샌드박스에서 차를 스폰해 타고 내리기(내리면 주차)

### 수정
- 편경사 구간 스폰: 스폰 높이를 도로 중심선이 아니라 그 차로의 노면에서 잼(바깥 차로가 높아 바퀴가 노면 안에 생겨 충격으로 펑크 나던 문제)
- 깊이 버퍼: WebGPU는 역방향 깊이(reversed-Z), WebGL2는 로그 깊이(0.1 m~32 km 시야). WebGL2에서 역방향 깊이를 켜면 하늘이 지형을 덮던 문제

## 배치 A — 내부 부품·성능 (진행 중)

### 추가
- **엔진·변속기 물리 부품(§4.4 내부 부품)**: 포르쉐의 엔진(215 kg, 3×3×3 노드 블록)과 PDK(125 kg, 2×2×3)를 격자에서 파낸 공간에 따로 두고 벨하우징 빔으로 결합, 고무 마운트 4개(엔진 뒤 2, 변속기 앞 2, 각 마운트는 모든 방향을 잡는 격자 노드 6~8개에 연결)에 매단다. 마운트는 진행성: ±15 mm 뒤 스너버(범위 밖에서만 작용하는 빔)가 받고, 한 연결이 25 kN을 넘으면 그 마운트 전체가 찢어진다. 구동 토크 반력을 변속기 케이스가 받아 풀 스로틀에 파워트레인이 9 mm 흔들리고, 64·80 km/h 벽 충돌은 스너버까지 밀리되 버티며, 100 km/h는 마운트 두 개가 찢어진다(`engine_mounts` 손상 그룹 → 뒤 하프샤프트 손실). 블록은 자체 자기충돌 그룹(9)이라 격자 공동 벽과 서로 부딪친다. 오일 팬 손상 그룹은 엔진 블록 아랫면 노드의 충격을, 변속기 그룹은 변속기 블록 노드의 충격을 감시한다. 웹: `visual.internals`의 모서리 노드 8개로 블록을 상자로 그림(떨어져 나가도 따라감)
- 빌더: `block()`(강체 부품 노드 블록), `anchors()`(3축을 모두 잡는 격자 연결점), `clearance()`(두 충돌 그룹의 정지 상태 여유 = 거리 − 접촉 도달 거리), `latticeSurface`의 `skipCell`(공동을 파낸 셀은 사면체를 만들지 않아 공동 벽이 비스듬한 면 없이 격자면이 됨)
- **휠·타이어 찌그러짐 표시(§6)**: 바퀴마다 트레드·림 링 4개(양쪽)의 노드를 허브 기준으로 32개 각도 구간에 다시 표본화(축에서의 거리 / 정지 거리, 축 방향 변위)해 정점 단계에서 림·스포크·타이어 메시를 그대로 휜다(림 반경 안은 허브 쪽으로 줄어듦, 좌우 링은 정점의 축 위치로 섞음). 연석에 휜 림, 충돌로 찌그러진 타이어가 노드 모양대로 보인다(web/src/vehicles/WheelDeform.ts, 64 km/h 오프셋 벽에서 부딪힌 바퀴 반경 20 % 찌그러짐·축 방향 5 cm)
- 앞 트렁크 뚜껑(보닛) 격자 피치 0.42 → 0.21 m(노드 18 → 76): 축 강성·항복력을 피치에 비례해 줄여 같은 강성·강도. 가벼운 패널 노드는 2 kHz 스텝 안정 한계까지 질량을 올림(+1.9 kg)

### 변경
- 자기충돌 후보 쌍(노드, 삼각형)을 그룹 쌍마다 Verlet 목록으로 캐시(여유 4 cm, g 노드가 여유의 1/4 이상 움직이거나 삼각형 목록이 갱신되면 재구성): 1대 주행 0.358 → 0.269 ms/스텝, 10대 충돌(1스레드) 5.49 → 3.92 ms/스텝
- 격자 노드 질량: 층 가중 분배에 더해 노드마다 m ≥ ½·Σk·(0.63 ms)² 하한(안정 한계). 무게 배분을 맞추는 추가 질량은 엔진룸 부속(양수) 또는 앞 트렁크(연료·라디에이터, 음수)
- 충돌 이벤트 Δv: 50 ms 창의 최대 변화 → 이벤트 시작 이후 실내 수평 속도 변화(표준 정의)
- 경첩 패널의 실(압축 전용 받침)은 패널 법선 안쪽 뒤의 격자 노드에만 연결(옆·위 노드에 연결되면 열릴 때 뚜껑을 붙잡음)
- 앞유리 균열 기준 프레임 소성 변형률 0.6 % → 0.2 %(파워트레인이 따로 매달려 64 km/h에서 프레임 변형이 0.29 %로 줄어듦, 40 km/h까지 0). 라디에이터 균열 시험 속도 15 → 20 km/h(15 km/h에서는 앞부분 소성 0.7 kJ로 균열 없음)
- 테스트 샤딩은 `--order decl`로(무작위 순서에서는 샤드가 겹치고 빠짐)

### 수정 (충돌 시험의 이상한 점)
- **차대차 에너지 수지 오차 8~28 %**: 두 차체 사이에 끼인 노드가 스텝마다 상대 면을 뚫고, 바디 CCD가 그 노드를 면 1 mm 앞(접촉 띠 깊숙이)으로 되돌려 스텝이 싣지 않은 스프링 에너지를 만들고(구속의 일로 기록), 다음 스텝에 다시 뚫리며 그 에너지가 기록 없이 사라지던 순환. CCD는 이제 이번 스텝의 접근만 되돌린다(스텝 시작 때의 면 앞 거리, 최소 1 mm). 스윕 뒤에도 면 뒤에 남은 노드의 스프링(스텝 끝 힘 법칙이 풀어 버리는 것)은 CCD 손실로 기록. 크래시 랩 정면 충돌 최악 |수지| 148 kJ(28 %) → 16 kJ(3 %), 회귀 테스트 추가
- **정면 충돌 뒤 뒷바퀴 토가 ±3° 틀어져 보이던 것**: 차체 기준 틀의 앞 기준 노드가 크럼플 구역(z = 1.5 m)에 있어 앞부분이 찌그러지면 틀이 돌아갔다(뒤 서스펜션 소성 0인데도 토·방향·속도가 틀어짐). 기준 노드를 실내 셀(z = 0.9 m)로
- **충돌 뒤 차가 내려앉는 흔들림이 '벽 충돌' 이벤트로 기록**: Δv 8 km/h(EDR 기록 기준) 미만이고 흡수 에너지가 거의 없는 이벤트는 충돌 로그에서 제외, 차 한 대 이벤트의 대상 표기 '벽' → '벽·지면'
- **에너지 추적이 물리 비용을 3배로**: 바디 CCD 직전의 위치에너지 측정을 두 바디(다른 가족)가 스윕 범위 안에 있을 때만 — 주행 0.82 → 0.31 ms/스텝(추적 끔 0.27)

## M2 — 충돌·파손 (2026-09-24)

### 추가
- **M2a 소성·크럼플**: 빔 `crushLimit`(소성 압축이 이 길이 비율 아래로 내려가지 않고 탄성으로 전환 — 노드가 서로 통과하지 않음), `tearLimit`(순 소성 연신 파단). 포르쉐 생성기가 단면 z마다 항복력 = 구역 단면 붕괴력 / Σ|cosθ|(앞 300 kN, 실내 900 kN, 뒤 400 kN). 토션 바가 비틀림 한계·레버 접힘에서 파손. 64 km/h 벽: 크럼플 0.62 m, 5 ms 평균 최대 31 g, 실내 1.6 cm, 수지 오차 0.34 %
- **M2b 차↔차 접촉**: 바디 충돌 삼각형(외피)·고유 모서리·자기충돌 그룹. 노드↔삼각형(면·모서리·꼭짓점 영역), 특징 모서리↔모서리, 표면 밖 노드는 구. 반력을 가중치로 나눠 선운동량 보존. 이동 삼각형 CCD(공면 3차식 이분법)·모서리 스윕(1 mm 이격, 최대 4회). 자기충돌(그룹 쌍 + Verlet 후보, 찌그러진·찢어진 삼각형 퇴역). 관통 계측(KICKOFF C2: 정적 노드, 광선 홀짝 내부 판정, 모서리의 면 관통)
- **M2c 섬 분리**: 빔 파단 후 연결 성분을 다시 계산해 가장 무거운 성분을 남기고 나머지를 새 바디로(상태·표면·압력 그룹 유지). 원 바디 노드 슬롯은 kDetached. 바퀴가 떨어지면 그 바퀴의 구동·제동·타이어 힘 제외, 섀시 기준점이 떨어지면 제어기 정지
- **M2d 접촉 연속성·벤치**: 새 접촉은 (직전 깊이 + 이번 스텝 접근 거리) 안에서만 인정 — 찌그러진 외피가 노드 위로 접히며 스프링이 깊게 '생기는' 에너지 생성 제거. `sbc-cli crash-bench` 10대 동시 충돌(정면 5쌍, 64 km/h): 관통 0, 네이티브 1스레드 2.36 ms/스텝, 4스레드 1.11 ms/스텝
- **M2e 플렉스바디**(web/src/vehicles/Flexbody.ts): GLB 정점을 가장 가까운 격자 노드 4개(역거리² 가중)에 바인딩, 노드 프레임 회전 M = R·R0ᵀ로 변형, 변형 야코비안의 여인수로 노멀 재계산(유한차분과 1e-6 일치), WebGPU·WebGL2 공용 TSL 정점 단계. 섬 분리 시 부품을 따라가고, 조각이 갈린 삼각형은 버림(찢어짐)
- **M2f 유리·램프**: 손상 그룹(빔 영구 변형률·파단 + 노드 충격) — 접합유리 거미줄 균열·처짐, 강화유리 소멸 + 알갱이 파티클, 램프 소등·렌즈 구멍 + 파편 파티클. 15 km/h 헤드램프, 64 km/h 앞유리 균열, 100 km/h 측면 유리 파쇄(실측 보정)
- **M2g 손상→기능 연동**: 냉각(서모스탯·공기유량·냉각수, 출력 제한, 과열), 윤활(유압·기아 마모 → 소착), 연료(소비·누유), 과회전 마모. damageLinks: 냉각수·오일·연료 누출, 조향(유격·쏠림·고착), 하프샤프트, 브레이크 라인, 변속기(상단 기어 상실), 전장(ABS·TCS·조명). 충돌 센서(50 ms Δv·10 ms G)와 에어백. 경첩 패널 4개(앞·엔진 후드, 도어 2: 래치·소성 힌지·압축 전용 실). 에어로 패널(평판 법선력 + 외판 흐름 흡입). 저사이클 피로(fatigueLimit). 계기판 게이지·경고등, 누유 파티클·얼룩, 에어백 표시
- **M2h 충돌 시험장** (`?crash=`): 시나리오(고정벽 풀랩·오프셋·차대차, 속도 프리셋 32~100 km/h, B 속도·각도·옆 오프셋), 발사 도구(충돌 0.3 s 전 위치에서 설정 속도, 모든 차가 놓일 때까지 시계 정지), 충돌 이벤트 로그(시각·대상·위치·상대 속도·최대 힘·흡수 kJ·최대 감속 g), 실시간 에너지·운동량 그래프, 슬로모션·투시·따라가기 카메라. 코어 충돌 센서의 이벤트 구간화(시작 = 직전 1 g 미만 표본)
- **M2j 도구(§20)**: 코어 테더(노드↔월드 점 또는 노드↔노드 스프링-댐퍼, 외부 일로 기록, 섬 분리 시 노드를 따라감). 웹: 노드 잡기(끌기·들어 올리기, 바디 무게 2배까지), 크레인·윈치(4 m 위 갈고리, 40 kN, 0.6 m/s 감기·풀기, 용량 초과 시 풀림)
- **M2k 타이어·휠 손상(§6)**: 스파이크 펑크, 핀치(바닥침)·블로우아웃, 비드 이탈, 림 휨(림 빔 소성 → 누출), 허브 베어링 끌림, 공기압에 따른 강성·그립·코너링 강성·구름저항, 펑크 주행 → 타이어 탈락 → 림 주행(불꽃). 계기판 타이어 공기압 경고, 평평해지는 타이어 표시, 떨어진 부품 표시(LooseParts). 주행 씬 타이어 시험 차로
- **M2l 공력(§10)**: 외피 삼각형별 압력·흡입·마찰(Cd·A와 축 양력 합계로 보정, 자세·요·옆미끄럼·파손에 따라 변함), 윙 요소(얇은 날개 양력·실속·유도 항력·각도 조절, 포르쉐 리어 스포일러), 슬립스트림(젠슨 후류 — 앞차가 끌고 가는 공기), 일정한 바람. 텔레메트리 항력·앞/뒤 다운포스·대기 속도
- **M2n 서스펜션 변형(§4.4)**: 서스펜션 암·토 링크·타이로드·서브프레임·피벗 차체 마운트에 항복·파단(가혹 주행 최대 하중의 약 2배에서 항복: 암 24 kN, 타이로드 14 kN, 토 링크 12 kN, 서브프레임·마운트 30 kN — 실측 최대 하중 11.4 / 7.4 / 4.1 / 6.6 / 15.7 kN). hydro 빔(타이로드)도 소성: 소성 변화를 작동 휴지 길이의 오프셋으로 유지. 휠 텔레메트리에 캠버·토(차체 기준). 25 km/h로 15 cm 연석에 옆으로 부딪히면 부딪힌 쪽(오른쪽) 캠버 앞 +3.6°·뒤 +2.8°, 토 +6.0°·+2.0°(반대쪽 소성 0), 휜 타이로드(6 mm)는 손 놓고 6초에 41 m 쏠림·직진 유지에 조향 −0.049. 충돌 시험장 패널에 휠 정렬·타이어 표(설계 정렬에서 1° 넘게 틀어졌거나 공기가 새는 바퀴 강조)
- **M2m 정적 월드 BVH(A§4.6)**: 정적 삼각형의 광역 단계를 선형 스캔에서 BVH(중앙값 분할, 잎 8개, double 경계)로. 질의 결과를 번호순으로 정렬해 선형 스캔과 해시가 같다(10.8k 삼각형 시험). 17.2만 삼각형에서 스텝당 4.82 ms → 0.22 ms

### 변경
- 접촉 모델(M2b~M2g): 접촉 스프링 질량 척도 Σw/m(미끄러질 때 강성 불변, 보존적), 통합 접촉 예산(정적·자기·바디가 노드당 2m을 나눔, 대칭 Gershgorin), 표면 조각별 가장 깊은 접촉 하나, 바디 가족(떨어진 부품과 원 바디는 스윕 없음·관통 집계 제외), 관통 판정 광선은 닫힌 표면만
- 정적 CCD(M2f·M2g·M2k): 투영점이 삼각형 밖이면 면의 평면 위로 착지, 에너지 보존형 착지 깊이. 장부는 '노드의 스텝을 대신한 구속'으로 — 스텝 시작 위치 → 착지 위치의 위치에너지 변화를 구속의 일로, 스텝 시작 속도 → 착지 속도의 운동에너지 감소를 CCD 손실로
- 텔레메트리 헤더 32 → 62(유체·에어백·충돌 이벤트·공력), 바퀴 레코드 20 → 30(타이어 손상, 캠버·토와 첫 스텝의 설계값)
- 바디 접촉의 표면 조각 깊이(M2n): 조각의 가장 깊은 삼각형 하나 → 조각 안 적중들의 매끄러운 최댓값 p = τ·ln Σ exp(pᵢ/τ)(τ 1 mm, 적중마다 몫 σᵢ로 작용). 동률이 아닐 때는 이전과 같고, 동률에서 τ·ln 2 더 깊다
- 주행 URL: `&at=x,z &yaw= &kmh=` 시작 자세. 차량 1대 표현을 `VehicleActor`로 분리(주행·충돌 시험 공유)
- 정적 충돌 형상을 재질별 색으로 그림(아스팔트·콘크리트·강철·스파이크)

### 수정
- 벽 충돌 뒤 앞바퀴가 찌그러진 휠하우스에 끼어 스스로 돌며 ≈190 W를 만들던 문제(접촉 강성 척도, M2e): 110 km/h 충돌 수지 3.4 % → 0.9 %
- 방지턱 모서리에서 정적 CCD가 노드를 동결시켜 속도가 500 m/s까지 누적(M2f)
- 정적 CCD가 압축된 빔을 거슬러 노드를 되돌리며 에너지를 만들던 문제(M2g)
- 사각 턱(15 cm)에 앞범퍼가 부딪치는 충돌의 수지 오차 +436 kJ(184 %): 모서리 근처에서 가장 가까운 면 깊이가 면 크기만큼 뛰는 적분 중간 위치를 장부에 넣었고, 부서지는 구조가 노드를 면에 계속 밀어 넣을 때 매 스텝 적분 오차가 쌓였다 → 1.1 %(M2k)
- 누유 얼룩·램프 파편이 흰색으로 그려지던 문제(인스턴스 색 버퍼를 첫 그리기 전에 생성)
- 충돌 잔해가 정지 상태에서 ≈ 140 W를 만들던 문제(M2n): 찌그러져 접힌 펑크 타이어 카커스 안에 끼인 섀시 노드가, 서로 마주 보는 이웃 삼각형 둘 중 더 깊은 쪽으로만 밀려 매 스텝 반대쪽으로 넘어가며 300 Hz로 떨었다(에너지 V자 골의 꺾임). 조각 깊이를 매끄러운 최댓값으로 바꿔 골 바닥을 둥글게 → 0.5 W. 같은 수정으로 풀 스로틀 벽 충돌 뒤 바퀴가 휠하우스를 갈 때의 수지 오차도 19.8 → 2.8 kJ
- **네이티브 ≠ WASM(차량)**: 골든 해시는 차 없는 장면만 검사하고 있었다. 차량 충돌 골든(`golden_crash_pair.txt`, 포르쉐 2대 정면)을 넣었더니 두 가지가 드러났다. (1) 차량 상태 해시가 `size_t`(wasm32에서 4바이트)를 그대로 넣어 같은 상태에서도 해시가 달랐다 → 64비트로. (2) libm 초월함수 `std::asin`(윙 받음각, M2l)·`std::hypot`(충돌 센서)·`std::log`(기체 에너지 장부)는 플랫폼마다 마지막 비트가 다를 수 있다 → `det::asin·exp·log`(IEEE 기본 연산만)와 √(x²+y²)로. 이제 10대 충돌 벤치도 네이티브 = WASM(1·4스레드 같은 해시)
- **바디 사이 관통(M2n 재검증)**: M2d의 '300~540 km/h 큐브 → 얇은 패널 1,000회 관통 0'은 시험 시드 하나에서만 성립했다(다른 시드는 1,000회에 2~6회 관통). 큐브 면이 대각선(평평한 내부 모서리)을 따라 10° 미만으로 살짝 접히면서, 그 면 평면에 거의 누운 패널 테두리 모서리 위로 접힘선이 넘어가 테두리가 두 삼각형을 뚫었다 — 모서리 스윕은 특징 모서리끼리만 봤다. 특징 × 평평 모서리 쌍도 스윕(스텝 한쪽 끝에서라도 공면에서 0.1 mm 이상 떨어진 결정적 교차만) → 8개 시드 × 1,000회 관통 0. 시험은 두 시드(2718, 11)로

### 해결
- KNOWN_ISSUES P1(노드 구↔노드 구 충돌만), P3(섬 분리 없음), P4(정적 삼각형 선형 스캔), P9(차체 단위 공력만), P18(차체 소성 없음), P19(차체 강체 표시) — M2에서 구현

### 골든 해시
- `golden_m0.txt` 재생성: M2b(접촉 모델), M2c(노드 플래그·슬라이더·압력 그룹 상태를 해시에 포함), M2d(접촉 깊이 상태), M2e(접촉 강성 척도), M2g(접촉 연속성·선택·예산, 해시에 접촉 쌍), M2l(마찰 짝힘), M2n(조각 깊이의 매끄러운 최댓값). 새 골든 `golden_crash_pair.txt`(M2n). 테더(M2j)는 있을 때만 해시에 들어가고, M2m(BVH)은 선형 스캔과 같은 해시. 이후 네이티브·WASM·브라우저 WASM 일치

### 변경 (차량 모델, 사용자 요청)
- 마이바흐 GLS: 테일램프 렌즈가 빠진 개구부(속이 비어 보이던 곳)를 뒤·옆을 감싸는 렌즈 면으로 채워 적색 발광, 그 아래 빨간 띠는 차체 색으로. 헤드램프 렌즈 개구부도 채움(아래 주간주행등 띠 + 위 은색 렌즈). 앞유리·파노라마 루프·뒷유리는 원본 조각과 합성 면을 한 장의 짙은 틴트 유리로
- 임포터: 임의 방향 시점의 빈 곳 채우기(램프를 정면으로 보는 대각선 시점, 좌우 대칭), '골짜기' 구멍 판정(가로·세로로 양쪽이 모두 높을 때만 구멍 — 비스듬한 면과 외곽선 제외), 램프 구역 배열(유리·트림별 색·명암, 다른 역할로 재지정)
- 롤스로이스 고스트: 맞춘 휠보다 3 cm 앞·5 cm 바깥에 남아 앞바퀴 앞으로 튀어나오던 베이크 타이어 조각 제거
- 유리 재질: 노드 재질에서 `envMapIntensity`가 장면 환경맵에 적용되지 않아 반사를 `specularIntensity`로 낮춤

## M1 — 첫 주행 (2026-09-23)

### 추가
- **M1a 요소**: 비등방 빔(압축·인장 강성 분리), hydro 빔(입력 채널 + 속도 제한), 슬라이더(노드-레일), 등온 압력 그룹(발산 정리 부피, 기체 위치에너지 계상), 토션 바(반력을 레버 발끝에 걸어 순수 우력), 결정론 `det::atan2` — 테스트 5종
- **M1b 차량 서브시스템** (`sbc/vehicle.h`): 하이브리드 타이어(노드 타이어 + 과도 슬립 Magic Formula), 엔진·자동 클러치·DCT형 변속기(자동/수동 시퀀셜)·구동계 와인드업·LSD, stick-slip 브레이크·핸드브레이크, ABS·TCS, hydro 조향, 차체 항력·다운포스, 텔레메트리. 차량 힘의 일은 에너지 원장에 외부 일·마찰로 정확히 계상
- 압력 휠 생성기(`addPressureWheel`), 절차 생성 기준차 **APEX Proto**(격자 차체, 4륜 더블 위시본, 안티롤바, 코일오버·범프스톱, 랙 조향, 1.19 t FR), `proto_drive` 씬, C ABI 차량 함수 6종
- 재질 쌍: 고무↔아스팔트·콘크리트·강철, 강철↔아스팔트(§11.1). `ContactPairParams`에 구름저항·트레드 하중 분담 추가
- **M1c 차량 데이터**: `apex-vehicle` JSON 포맷(docs/VEHICLE_FORMAT.md)과 코어 로더(yyjson, 결정론 파싱, JSON 경로를 담은 오류), C ABI `sbc_world_spawn_vehicle_json`, `sbc-cli vehicle`(안정성·정착·하중 분배 보고)
- **포르쉐 911 터보(991) 생성기** (`tools/vehicle-gen`): GLB 차체 헐로 자른 격자 섀시, 맥퍼슨 스트럿(슬라이더 톱 마운트)·5링크 후륜, 조향 고정 기구학 풀이로 구한 범프 스티어 0 타이로드·토 링크 점, 승차 주파수 스프링, 1,595 kg·앞 39 % 질량 풀이, 7단 PDK·AWD 30/70·후륜 LSD → 799 노드·5,846 빔, 안정성 통과, 정착 후 앞 하중 39.0 %
- 차량 테스트 8종: 안정성, 정지 유지, 30% 경사 핸드브레이크, 100→0 km/h ABS 제동(41~43 m, 직진), TCS 발진, 코스트다운 에너지 수지, 조향 방향, 스레드 수 무관 결정론. 네이티브·WASM 해시 일치
- **M1d 튜닝·§23.2 시험(포르쉐)**: 100→0 km/h 35.1 m, 0→100 km/h 3.31 s(목표 3.4 s), 정상 스키드패드(R 30.5 m) 0.98 g, 코스트다운 99→80 km/h 19.6 s(도로 부하 모델 19.5 s), 30% 경사 5초 밀림 0.1 mm, 200 km/h대 휠 홉 없음 — 테스트 6종 추가(`[porsche]`)
- 능동 센터 커플링(`centreCoupling`, PTM/할덱스형 다판 클러치: 뒤 차축 직결, 앞 차축 몫이 축 하중을 따르고 토크는 빠른 쪽에서 느린 쪽으로만 흐름)
- TCS 피드포워드를 "가장 먼저 한계에 닿는 구동 바퀴" 기준으로 바꾸고, 슬립 적분 보정이 한계가 실제로 걸릴 때만 1.5배까지 올라가게 함
- 타이어 반경 스프링(`verticalStiffness`, `radialDamping`): 허브 높이로 처짐을 재고 허브에 건다. 스포크 강성(`spokeStiffness`)을 림 링과 분리
- 포르쉐 생성기: 서브프레임, 차축 뒤 토 링크, 수평 하부 암·작은 킹핀 오프셋의 맥퍼슨, 타이어 µ 1.18, 무거운 허브 노드, 트레드 접촉 구 5 cm
- **M1e 웹 주행** (`?drive=<차량>`): 워커의 차량 스폰(`spawnVehicle`, JSON 경로는 문서 기준 절대 URL)·입력(`vehicleInput`)·텔레메트리(SAB 트리플 버퍼의 차량 구역, 프레임 보간), 키보드·게임패드 입력(`DriveInput`: 속도 감응 조향 제한, 정지 중 뒤 페달 0.35초 유지로 후진, E/Q 변속, M 자동/수동, T·B TCS/ABS, R 재시작, C 카메라, V 엑스레이), 추적·궤도 카메라, GLB 차체를 섀시 좌표계에, 휠을 물리 허브에 결합(`VehicleView`), 계기판(속도·기어·회전계·페달·보조 장치 램프), 샌드박스 패널의 주행 시작
- 주행 씬 `drive`: 아스팔트 평지, 옆 차선 고무 콘 6개, 방지턱 3개, 1.2 m 점프 램프, 300 m 앞 콘크리트 벽
- 차고 뷰: 측면·정면 준정사영(`cam=side|front`)과 임의 시점(`cam=x:y:z`), 거리 라벨 끄기(`labels=0`)
- **차량 모델 재설계(사용자 Crash Lab 모델 기준)**: 임포터가 옆에서 본 깊이 래스터로 휠 아치 립을 찾아 차축별 타이어 크기와 위치를 맞추고, 남는 틈만큼 차체를 내린다(`fitWheels`). 절차 휠 스타일(고스트 7 트윈 스포크, 마이바흐 20 스포크)과 브레이크 디스크·캘리퍼
- 마이바흐 GLS: 23인치 규격 타이어(285/40 R23)로 차고 118 mm 낮춤, 휠베이스 3.47 → 3.14 m(실차 3.135 m). 베이크에 없던 앞유리·파노라마 루프·뒷유리를 주변 프레임을 잇는 막면(조화 보간)으로 채우고, 그릴·흡기구 뒤와 헤드램프 윗틈을 막음. 헤드램프 = 다크 크롬 하우징 + 주간주행등, 테일램프 = 적색 렌즈 + 스모크 상단(자체 발광)
- 차고 아티팩트 뷰어(`tools/artifact`): 앱의 모델 로더를 그대로 묶은 3D 차고(물리 없음), 마이바흐 수정 전후 비교
- **교차 출처 격리 없는 페이지 지원**: 단일 스레드 WASM 모듈(`sbc-st`, CMake `SBC_WASM_THREADS=OFF`, 프리셋 `wasm-st-release`)과 슬롯 버퍼를 전송하는 메시지 트랜스포트. SharedArrayBuffer가 없으면 자동 전환한다(이전에는 안내문만 띄우고 멈췄다)
- 아티팩트 앱 패키지(`tools/artifact/package-app.mjs`): 해시 경로(#drive·#drive.<차량>·#garage·#sandbox), GLB base64 로딩(`VITE_APEX_GLB_BASE64`), WebGL2 고정
- 테스트: 드라이브 씬 벽 충돌 에너지 수지(`[crash]`), 지면에 눌린 노드의 CCD 미끄러짐. E2E 주행 검사(발진·변속·조향·제동·전복 없음)

### 변경
- 임포터: 베이크 정점색을 sRGB → 선형으로 변환(glTF 규약). 모든 차 표면을 양면 렌더(사용자 Crash Lab 렌더러와 같음)
- 고무↔아스팔트 명시 구름저항 0.010 → 0.0097, 고무↔콘크리트 0.009 → 0.0092(트레드 자체 손실을 합쳐 25 m/s에서 약 0.012). 저속 구름저항 모멘트의 정칙화 구간 0.3 → 0.05 m/s
- APEX Proto: 하드포인트 결속 3e5 → 6e5 N/m(질량 2 kg). 핸드브레이크 경사 시험은 착지 흔들림이 가라앉은 4초 뒤부터 잰다

### 수정
- **CCD가 지면에 눌린 노드를 얼려 에너지를 만들던 문제**: 클램프가 충돌 시점으로 되감으면서 그 스텝의 접선 이동까지 지웠다. 매 스텝 지면 아래로 눌리는 노드(착지 충격을 받은 타이어 트레드 노드)는 제자리에 멈춘 채 접선 속도만 수백 m/s로 커졌고, 빔 감쇠가 그것을 차 전체로 옮겨 0.1초 만에 23 MJ의 수지 오차를 냈다. 이제 끝점을 표면에 투영해 침투만 지운다(삼각형 밖으로 나가면 충돌 시점으로 대체). 90 km/h 전속 벽 충돌의 수지 오차 5.4 kJ(최대 운동에너지의 0.7%)
- **작업 스레드 풀의 시작 경쟁으로 인한 멈춤**: 워커가 스케줄된 뒤에 세대 번호를 읽어, 새 스레드가 돌기 전에 첫 병렬 구간이 시작되면 그 구간을 이미 본 것으로 여기고 참여하지 않았다. 호출 스레드는 그 워커를 영원히 기다렸다(부하가 걸린 기기에서 스레드 수 결정론 테스트가 멈춤). 이제 생성 시점의 세대 번호로 시작한다. 부하 중 병렬 12회 반복 통과
- 주행 모드에서 샌드박스 패널이 `hidden`인데도 보이던 문제(`display` 규칙이 `hidden`을 덮음)
- 주행 씬의 콘이 노드 반경만큼 지면에 묻힌 채 생성돼 첫 스텝에 30 m 튀어 오르던 문제
- 램프 쐐기의 아랫면(한쪽 면 정적 삼각형)이 반경 안의 트레드 노드를 아래로 끌던 문제: 아랫면 제거, 허브에서 멀어지는 쪽을 향한 트레드 접촉은 노면 평면 재구성에서 제외
- 고속(약 210 km/h 이상)에서 네 바퀴가 같은 위상으로 10 Hz 홉을 일으키던 문제. 원인은 반경 스프링이 링 질량 중심의 높이를 쟀던 것(접지면이 눌리면 질량 중심이 올라가 스프링이 눌릴수록 약해짐)
- 고속 휠 부풂(스포크 1e5 N/m로 림이 300 km/h에서 2.6 cm 성장) → 최고속 233 → 약 290 km/h(KNOWN_ISSUES P16)
- 앞바퀴 구동력에 의한 차체 재킹·3 Hz 피치 한계 사이클(하부 암 처짐 12°, 킹핀 오프셋 11 cm)
- 후륜 토 플러터(토 링크가 차축 앞에 있어 컴플라이언스 스티어가 불안정), 하드포인트·격자 컴플라이언스

### 해결
- KNOWN_ISSUES P8(비등방·hydro·압력 그룹 없음) — M1a에서 구현

## M0 — 기반 (2026-09-23)

### 추가
- **SoftBodyCore (C++20)**: SoA 노드·빔(normal·support·bounded·rope), symplectic Euler 2000 Hz, 경화를 포함한 소성 복귀 사상, 파단 힘·변형 한계·breakGroup
- 접촉: 질량 비례 페널티 스프링-댐퍼 + stick-anchor 쿨롱 마찰(정지/운동 분리, 변위 기반 앵커) — 지면 평면, 정적 삼각형 메시, 노드 구 바디↔바디. 정적 표면 CCD
- 계측: 운동·중력·빔·접촉 위치에너지, 빔 감쇠·접촉 감쇠·마찰·소성·파단·CCD 손실, 외부 일 → 에너지 수지. 선·각운동량. FNV-1a 상태 해시
- 결정론: 고정 순서 병렬(섬 단위 fork-join), 자체 sin/cos, FMA 축약 금지, 바디별 double 원점 + 4 m 재기준
- 안정성 검사기(감쇠 보정 빔 조건 + Gershgorin 노드 조건), 격자 구조물 생성기, 기준 씬 6종, `sbc-cli`(run·bench·stability·golden), C ABI
- 테스트 30종(Catch2): §23.1 자유낙하·진동 주기·감쇠 진동·운동량·결정론(10회, 1·2·4 스레드, 골든 해시)·10분 정지·30% 경사 정지·안정성 한계·64 km/h 에너지 수지·빔·CCD — 네이티브와 WASM 모두 통과
- **웹**: 물리 워커(WASM SIMD128 + pthreads), SharedArrayBuffer 락프리 트리플 버퍼, 프레임 보간, 과부하 시 슬로다운(RTF 표시), 시간 제어(일시정지·한 스텝·1/2~1/100)
- 무한 그리드맵(1 m/10 m 격자, 거리 표시), 노드·빔 응력 색 디버그 렌더, 정적 충돌 형상 렌더, 샌드박스 패널·HUD(한국어 기본), 데이터 드리븐 스폰 프리셋
- `?golden=1` 브라우저 결정론 자가 검사, `?bench=1` 벤치 JSON, E2E 스크립트, CI 워크플로

### 골든 해시
- `core/tests/golden/golden_m0.txt` 최초 생성. 네이티브(GCC 13, x86-64), WASM(Node 22), 브라우저 WASM(Chromium 141, 2스레드)에서 모두 일치
