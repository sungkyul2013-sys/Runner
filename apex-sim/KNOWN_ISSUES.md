# 알려진 문제·미구현 (§1.8)

"구현된 척 금지" 원칙에 따라 임시 구현, 제약, 미구현 항목을 모두 여기에 적는다. 해결되면 항목을 지우고 CHANGELOG.md에 기록한다.

## 물리 코어

| # | 내용 | 영향 | 계획 |
|---|---|---|---|
| P1 | 바디↔바디 충돌은 **노드 구↔노드 구** 페널티만 구현했다(노드↔삼각형, 모서리↔모서리 없음). 노드 간격보다 반경이 작으면 고속 충돌 때 격자끼리 일부 파고든다. M0 큐브는 반경을 격자 간격의 절반 가까이(0.1 m / 0.25 m)로 잡아 완화했다 | 차↔차 충돌 품질 | M2: 노드↔삼각형 + 모서리↔모서리 + CCD (§5.2) |
| P2 | 마찰을 노드 중심(점질량)에 가하므로, 실제 접촉점과의 거리(접촉 반경)만큼 가짜 우력이 생긴다. **마찰이 있는 바디↔바디 충돌에서는 각운동량이 보존되지 않는다**(시험 장면에서 약 20%). 선운동량은 1e-7 수준으로 보존되고, 마찰이 없으면 각운동량도 1% 이내로 보존된다(테스트로 확인) | §5.3 각운동량 보존 | M2: 노드↔삼각형 접촉으로 팔 길이를 노드 반경 수준으로 축소. 잔여량은 측정해 재보고 |
| P3 | 빔이 끊어져도 **연결 성분 분리(독립 섬)는 하지 않는다**. 떨어진 조각은 같은 바디로 남고 자기충돌이 없어 서로 통과한다 | 파손 연출 | M2 (§4.3 분리 부품) |
| P4 | 정적 삼각형 광역 단계가 **선형 탐색**이다(바디마다 전체 정적 삼각형 AABB 검사) | 대형 맵 성능 | M3/M6: 셀 단위 BVH + 스트리밍 (A§4.6) |
| P5 | float32 좌표의 한계 때문에 노드가 스텝당 ½ ulp보다 적게 움직이면 위치가 멈춘 채 **잔여 속도**가 남는다. 상한은 ulp(x)/(2·dt)이다. 4 m 재기준(rebase)으로 좌표를 작게 유지해 자동차 크기 바디에서는 약 0.5 mm/s 이하로 묶었다 | 정지 차량 텔레메트리의 미세 속도 | 필요하면 보상 합산(Kahan) 위치 적분 검토 |
| P6 | 페널티 접촉이 시작되는 스텝의 적분 오차 때문에 큰 충격 1회당 충격 에너지의 약 1%가 수지 오차로 남는다(큐브 낙하 0.96%, 64 km/h 크래시 0.34%, 합격선 5%) | 에너지 수지 정밀도 | 접촉 시점 보간 검토 (M2) |
| P7 | 빔 커널이 아직 **스칼라 코드**다(수작업 SIMD 아님) | 성능 여유 | M2 크래시 벤치 이후 SIMD128/AVX2 커널 |
| P9 | 공력은 **차체 단위 항력(Cd·A, 질량 중심)과 앞·뒤 축 다운포스**만 있다. 삼각형별 항력·양력, 슬립스트림, 파손에 따른 공력 변화는 없다 | 고속 주행·드래프팅 | M2 (§10 삼각형 공력) |
| P10 | 이산 트레드(바퀴당 2열×24 노드)가 접지면을 지나며 생기는 **속도 의존 구름 손실**이 명시한 구름저항 외에 따로 있다. 등가 Crr로 25 m/s에서 약 0.0023, 55 m/s에서 약 0.006이다. 노면 쌍의 명시 Crr(아스팔트 0.0097)은 25 m/s에서 합계가 §11.1 여름 타이어의 약 0.012가 되도록 잡았다. 그래서 고속에서는 실제보다 구름 손실이 조금 크다 | 코스트다운(§23.2)·최고속 | 포르쉐 코스트다운 99→80 km/h는 도로 부하 모델(Crr 0.012, CdA 0.65) 대비 +0.8%(테스트). 개선안: M2 타이어(세그먼트 증가, 트레드 링 암시 적분) |
| P11 | 하이브리드 타이어의 **하중 경로**: 트레드 노드 접촉 스프링의 10%만 노드에 걸리고, 나머지 하중은 허브에 거는 반경 스프링(`verticalStiffness`)이 진다. 트레드 노드가 한 개씩 노면을 때리며 생기던 큰 에너지 손실을 없애기 위한 설계다. 그래서 하중에 따른 **카커스 변형이 실제보다 작고**, 정지 시 트레드 노드 구가 노면에 약 1.2 cm 파고든다(디버그 뷰에서 보임). 엔벨로핑(여러 노드 접촉 합산)과 림 충돌은 유지된다 | 타이어 변형 연출·연석 충격 | M3: 노면 요철·연석 시험으로 비율 재조정 |
| P12 | 노드 타이어는 2 kHz 명시 적분의 안정 한계 때문에 **실제 공기압(2.5 bar)을 벨트 후프 장력으로 지탱하지 못한다**(EA가 MN급이면 dt가 µs급). 공기압의 구조 효과는 사이드월 강성으로 표현하고, 공동(cavity)은 1 bar 미만의 구조 게이지압으로만 돈다. 공기압에 따른 강성·접지면 변화, 펑크·비드 이탈은 아직 없다 | §6 공기압·파손 | M3/M4 (§6 열·공기압·파손) |
| P13 | 구동계는 **집중 비틀림 스프링 하나**(클러치+구동축)와 고정 토크 배분(오픈 디퍼렌셜) 또는 능동 센터 커플링(다판 클러치, 앞 차축 몫이 축 하중을 따름) + 클러치식 LSD다. 반축별 와인드업·휠 홉, 토센·비스커스·전자식 디퍼렌셜, H패턴 수동·토크컨버터·CVT, 엔진 크랭크 반력(가속 시 롤), 과급·연료·열 모델은 없다. 변속기는 DCT형 자동(D/R/N/수동 시퀀셜 모드, 자동 클러치)만 있다 | §8 파워트레인 | M3~M4 |
| P14 | 타이어의 열·마모·공기압 변화·수막현상, 캠버에 따른 접지점 스크럽 속도(휠 롤 속도 × 반경)는 없다. 접지 속도는 휠 질량 중심 속도로 잰다 | §6 | M3/M4 |
| P15 | 전자장치는 ABS(슬립 추종)와 TCS(가장 약한 구동 바퀴의 µ·Fz 토크 한계 + 슬립 적분 보정)만 있다. ESC·런치 컨트롤·크루즈·경사로 밀림 방지·경고등은 없다 | §9 | M4 |
| P16 | **최고속이 목표에 못 미친다**(포르쉐 약 290 km/h, 목표 315 km/h). 약 275 km/h부터 트레드 링의 반경 진동이 접지면 통과와 맞물려 휠 홉이 커진다(실제 타이어의 스탠딩 웨이브에 해당하는 현상이 너무 낮은 속도에서 나타남). 원인은 명시 적분 안정 한계 안에서 줄 수 있는 링 반경 강성이 실제보다 낮다는 점이다. 280 km/h에서 원심 팽창이 약 2.5 cm로, 실제 Y 등급 타이어의 약 0.5 cm보다 크다. 200 km/h대까지는 홉 없음을 테스트로 지킨다 | 최고속 주행 | M2 타이어: 트레드 링의 반경 자유도를 암시 적분하거나 서브스텝으로 나누고, 세그먼트를 늘린다 |
| P17 | 고속으로 **스폰**하면(약 50 m/s 이상) 휠 링이 원심 하중을 한 스텝에 받아 1~2초 동안 요동하고 차가 튄다. 링을 원심 평형 반경으로 미리 늘려 두지 않기 때문이다. 정지 상태나 100 km/h 이하 스폰은 문제없다 | 테스트·리플레이 시작 | 필요하면 스폰 시 링 반경을 원심 평형으로 초기화 |
| P18 | 차체에 **소성·크럼플 모델이 아직 없다**(M2). 포르쉐가 50 km/h로 벽에 부딪히면 탄성체처럼 약 30 km/h로 튕겨 나온다(반발 계수 약 0.6). 에너지 수지는 지킨다(90 km/h 전속 충돌 오차 0.7%, `[crash]` 테스트) | 충돌 연출·손상 | M2: 섀시 격자 소성·파단 + 크럼플 존, 부품 분리 |
| P19 | 주행 화면의 차체 메시는 **강체로 섀시 좌표계를 따라간다**(노드 변형이 비주얼에 반영되지 않음). 휠은 물리 허브·조향·캠버·회전각을 그대로 따른다 | 충돌 시 찌그러짐 표시 | M2: GPU flexbody(A§5) |

## 웹 클라이언트

| # | 내용 | 영향 | 계획 |
|---|---|---|---|
| W1 | 이 개발 컨테이너의 헤드리스 Chromium 141(SwiftShader)에서는 **WebGPU 캔버스 디바이스가 1초 안에 유실된다**. three.js 없이 순수 WebGPU clear 루프만으로도 재현된다. 그래서 E2E와 스크린샷은 three의 **WebGL2 백엔드**(`?backend=webgl2`)로 찍었다. 물리·워커·WASM은 백엔드와 무관하게 같다 | 헤드리스 WebGPU 검증 불가 | 실제 GPU 브라우저에서 확인 필요(사용자 PC 벤치) |
| W2 | three r186은 텍스처 뷰에 항상 `swizzle: 'rgba'`(항등)를 넘기는데, Chromium 141 이하는 이를 거부해 매 프레임이 실패한다. 항등 swizzle만 제거하는 호환 심(`web/src/render/webgpuCompat.ts`)을 넣었다(렌더 결과는 같다) | 구형 브라우저 호환 | three·브라우저가 갱신되면 제거 |
| W3 | WebGPU 디바이스가 유실되면 WebGL2 백엔드로 **페이지를 다시 연다**(시뮬레이션 상태는 사라진다) | 드문 GPU 리셋 시 상태 손실 | M5: 상태 스냅샷 복원 |
| W4 | 렌더링 성능은 GPU 없는 컨테이너에서 측정할 수 없다. `bench/results/M0-web-headless-swiftshader.json`의 프레임 시간(0.4 fps)은 CPU 래스터라이저 수치라 **의미가 없다**. 물리 수치만 유효하다 | §21.7 렌더 목표 미검증 | 사용자 PC에서 `?bench=1` 실행 필요 |
| W5 | M0 패널·HUD는 디버그 UI다. 디자인 시스템, 게임패드 포커스, 설정 화면은 M5 | UX | M5 (§18) |
| W6 | 실시간 입력 링(SPSC)은 아직 없다. M1e 차량 입력은 렌더 프레임마다 postMessage로 보낸다(지연 최대 1프레임 + 워커 스텝 경계). 리플레이 결정론은 입력이 워커에 도착한 스텝 기준이다 | 입력 지연·리플레이 | M5: SAB 입력 링 + 스텝 번호 타임스탬프 |
| W7 | 번들의 three.js 청크가 약 940 kB(gzip 260 kB)라 Vite 경고가 나온다 | 초기 로딩 | M5에서 코드 분할 |
| W8 | 교차 출처 격리(COOP/COEP)가 없는 호스트(Claude 아티팩트, 일반 정적 호스팅)에서는 SharedArrayBuffer를 쓸 수 없어 **단일 스레드 WASM + 메시지 전송**으로 돈다. 포르쉐 1대 주행은 실시간(스텝 약 0.33 ms, 예산 0.5 ms)이지만 무거운 장면(큐브 더미 등)은 시간 감속된다. 아티팩트 호스트는 .glb를 제공하지 않아 모델을 base64 텍스트로 싣고(약 33% 커짐), 번들 글꼴 대신 Google Fonts를 쓴다 | 성능·로딩 | 격리된 호스팅(`dist/_headers`)이 기본 배포 경로 |
| W9 | 주행 차량은 **포르쉐 911 터보 하나**다. 롤스로이스 고스트·마이바흐 GLS는 시각 모델만 있다(물리 차량 JSON 없음) | 차량 선택 | 생성기로 두 차의 물리 차량 생성 |

## 프로젝트·에셋

| # | 내용 |
|---|---|
| A1 | 새 저장소를 이 세션에서 만들 수 없었다(GitHub 통합 권한 403). 그래서 `Runner` 저장소의 `apex-sim/` 폴더에서 개발 중이다. 빈 저장소가 생기면 히스토리를 보존해 옮긴다(`git subtree split --prefix apex-sim`) |
| A2 | 사용자 지정 예외: 포르쉐 911 터보, 롤스로이스 고스트, 마이바흐 GLS는 사용자가 이전에 만든 모델(실존 차량 디자인)을 다듬어 쓴다(§1.7 오리지널 IP 원칙의 예외, 사용자 승인). 그중 911과 GLS의 **원본 출처·라이선스는 기록이 없다**(과거 저장소에는 "사용자 제공"으로만 남아 있다). 공개 배포 전에 확인이 필요하다 |
| A4 | 가져온 사용자 모델의 흠과 보정: 마이바흐 GLS 베이크에는 앞유리·파노라마 루프 유리·뒷유리가 없고 그릴·헤드램프 뒤가 비어 있다. 임포터가 이 자리를 **합성한 면**(주변 프레임을 잇는 막면, 어두운 백킹)으로 채운다. 스캔 데이터가 아니므로 곡률은 근사다. 휠은 두 차 모두 베이크 메시 대신 절차 휠을 아치 실측으로 맞춘다(마이바흐 285/40 R23, 휠베이스 3.144 m / 실차 3.135 m, 고스트 255/45 R20·285/40 R20). 고스트 보닛은 정점 색으로 어둡다(M4 카 페인트 셰이더에서 도색 처리). 포르쉐 램프 정점색은 베이크 그대로(베이지)다. |
| A3 | 네트워크 정책으로 Poly Haven·ambientCG에 접근할 수 없다(KICKOFF Q5). 에셋은 절차 생성 + `TEMP_` 슬롯으로 진행한다 |
