# 알려진 문제·미구현 (§1.8)

"구현된 척 금지" 원칙에 따라 임시 구현, 제약, 미구현 항목을 모두 여기에 적는다. 해결되면 항목을 지우고 CHANGELOG.md에 기록한다.

## 물리 코어

| # | 내용 | 영향 | 계획 |
|---|---|---|---|
| P2 | 마찰 짝힘(M2l)으로 노드↔삼각형·모서리↔모서리 마찰의 가짜 모멘트를 상쇄해 **마찰 있는 바디 충돌의 각운동량 오차가 20 % → 0.00 %**가 됐다. 다만 **두 점 노드끼리의 구 접촉**(표면 밖 노드)은 짝힘을 실을 관성이 없어 모멘트가 남는다. 정적 지면·벽과의 마찰은 외력이라 해당 없음 | 표면 없는 노드끼리의 마찰 충돌 | 필요하면 노드 구 접촉 마찰을 표면 접촉으로 대체 |
| P5 | float32 좌표의 한계 때문에 노드가 스텝당 ½ ulp보다 적게 움직이면 위치가 멈춘 채 **잔여 속도**가 남는다. 상한은 ulp(x)/(2·dt)이다. 4 m 재기준(rebase)으로 좌표를 작게 유지해 자동차 크기 바디에서는 약 0.5 mm/s 이하로 묶었다 | 정지 차량 텔레메트리의 미세 속도 | 필요하면 보상 합산(Kahan) 위치 적분 검토 |
| P6 | 페널티 접촉이 시작되는 스텝의 적분 오차 때문에 큰 충격마다 충격 에너지의 약 1 %가 수지 오차로 남는다(큐브 낙하 0.96 %, 64 km/h 크래시 0.34 %, 100 km/h 벽 0.2~0.4 %, 15 cm 사각 턱에 앞범퍼 충돌 1.1 %, 합격선 5 %) 차대차 정면 충돌에서는 두 차체 사이에 끼인 노드의 CCD 순환 때문에 순간 수지가 크래시 랩 기준 최대 약 3~7 %까지 흔들린다(CCD가 이번 스텝의 접근만 되돌리고 면 뒤에 남은 스프링을 CCD 손실로 기록하기 전에는 최대 28 %). 초기 조건에 민감(혼돈적)하다 | 에너지 수지 정밀도 | 접촉 시점 보간 검토 |
| P7 | 빔 커널이 아직 **스칼라 코드**다(수작업 SIMD 아님). 10대 동시 충돌 벤치(M2 최종)는 네이티브 4스레드 2.47 ms/스텝(예산 0.5 ms의 약 5배), WASM 4스레드 3.21 ms/스텝. M2d(1.11 ms) 이후 경첩 패널·유체·타이어 손상·공력·접촉이 더해져 느려졌고, 프로파일상 **자기충돌 열거**(문·후드 그룹이 늘 차체 그룹과 겹쳐 매 스텝 공간 해시 질의)가 스텝의 절반 가까이다 | 다수 차량 실시간(KICKOFF Q3) | M6: 자기충돌 노드↔삼각형 후보 캐시(Verlet)·잠든 패널·물리 LOD, M10: SIMD128/AVX2 커널 |
| P10 | 이산 트레드(바퀴당 2열×24 노드)가 접지면을 지나며 생기는 **속도 의존 구름 손실**이 명시한 구름저항 외에 따로 있다(등가 Crr 25 m/s 약 0.0023, 55 m/s 약 0.006). 고속에서 구름 손실이 조금 크다 | 코스트다운·최고속 | M3 타이어 |
| P11 | 하이브리드 타이어의 **하중 경로**: 트레드 노드 접촉 스프링의 10 %만 노드에 걸리고 나머지는 허브 반경 스프링이 진다. 그래서 **모서리(연석·사각 턱)에서도 타이어가 평평한 노면처럼 단단해** 국부 변형이 실제보다 작다(15 cm 사각 턱 60 km/h: 국부 처짐 5.2 cm). 핀치 펑크(M2k)는 이 모델의 트레드 노드가 림 반경 5 mm 안에 들어오는지로 판정하므로, 실제보다 조금 더 센 충격에서 펑크가 난다 | 타이어 변형·연석 펑크 문턱 | M3: 노면 요철·연석 시험으로 비율 재조정 |
| P12 | 노드 타이어는 2 kHz 명시 적분의 안정 한계 때문에 실제 공기압(2.5 bar)을 벨트 후프 장력으로 지탱하지 못한다. 공기압의 구조 효과는 사이드월 강성 + 허브 반경 스프링으로 표현하고, 공동은 1 bar 미만의 구조 게이지압이다. 공기가 빠지면(M2k) 셋 모두를 비율로 줄이고, 다 빠지면 공동을 연다. 공기압 표시는 공칭값(2.5 bar) × 남은 비율이다. 열에 따른 공기압 변화는 없다 | §6 공기압 | M3/M4 (§6 열) |
| P13 | 구동계는 집중 비틀림 스프링 하나(클러치+구동축)와 고정 토크 배분 또는 능동 센터 커플링 + 클러치식 LSD다. 반축별 와인드업·휠 홉, 토센·비스커스·전자식 디퍼렌셜, H패턴 수동·토크컨버터·CVT, 엔진 크랭크 반력, 과급·열 모델은 없다 | §8 파워트레인 | M3~M8 |
| P14 | 타이어의 열·마모·종류·수막현상, 캠버에 따른 접지점 스크럽 속도는 없다. 열로 인한 블로우아웃도 없다(M2k의 블로우아웃은 핀치·충격만) | §6 | M3/M4 |
| P15 | 전자장치는 ABS·TCS만 있다. ESC·런치 컨트롤·크루즈·경사로 밀림 방지는 없다. 경고등은 수온·유압·연료·브레이크·조향·구동·변속기·배터리·엔진·타이어 공기압(M2g·M2k) | §9 | M4·M5 |
| P16 | **고속 한계**: 바퀴 후프 장력(배치 C)으로 허브 압축과 업라이트 좌굴은 없어졌다. 911은 약 290 km/h까지 매끈하지만 **약 295 km/h부터 11 Hz 휠 홉 한계 순환**이 생겨 300 km/h 부근에서 떨리고 스핀한다(목표 최고속 315 km/h). 고스트는 283 km/h까지 안정. **마이바흐는 약 195 km/h부터 4 Hz 롤·요 위브**가 서서히 커진다(키 큰 차체 격자의 비틀림과 롤 조향의 결합으로 보이며 스태빌라이저·조향 강성·공력을 바꿔도 그대로 — 실제 차는 능동 롤 제어) | 최고속 주행 | M3 타이어(트레드 링 반경 자유도의 암시 적분 또는 서브스텝), 마이바흐 차체 비틀림 강성·능동 롤 모델 |
| P17 | 고속으로 **스폰**하면(약 50 m/s 이상) 휠 링이 원심 하중을 한 스텝에 받아 1~2초 요동한다 | 테스트·리플레이 시작 | 필요하면 스폰 시 링 반경을 원심 평형으로 초기화 |
| P20 | **정적 CCD 장부**(M2k): 보정을 '노드의 스텝을 대신한 구속'으로 기록한다(스텝 시작 → 착지 위치의 위치에너지 변화는 구속의 일, 시작 → 착지 속도의 운동에너지 감소는 CCD 손실). 보정된 노드가 스텝 중 받은 감쇠력의 일은 움직인 것으로 기록된 채 남는다(작음). 바디 사이 스윕(이동 삼각형 CCD)은 아직 적분 뒤 위치 기준으로 기록한다 | 수지 정밀도(극단 충돌) | 바디 스윕도 같은 방식으로 |
| P21 | 찌그러진 잔해의 **오목한 주름에 끼인 가벼운 노드**가 두 면 사이를 오가며 떨 수 있다. M2g에서 ≈ 0.1 kW까지 측정됐고, 접촉 선택·예산·연속성 수정, 납작한 타이어 공동 개방(M2l), 조각 깊이의 매끄러운 최댓값(M2n, 접힌 카커스 안의 노드 — 0.14 kW → 0.5 W)으로 줄였다. 현재 100 km/h 벽 충돌 잔해의 정지 5 s 표류는 수 J 수준이다. 다른 충돌 형상에서 다른 원인으로 다시 나타날 수 있어, 정지 시험의 한계(100 J)로 감시한다 | 잔해 정지 상태의 에너지 표류 | 재현 형상이 나오면 원인별로 |
| P26 | 서스펜션 변형(M2n)은 **축 방향 빔의 소성**(길이 변화)이다. 굽힘 모멘트로 휘는 암의 형상, 볼 조인트·부싱 파손, 너클(업라이트) 변형은 따로 없다(너클은 탄성). 휜 정렬로 생기는 **편마모**는 타이어 마모 모델이 없어 아직 없다(P14). 정렬 값은 텔레메트리·충돌 시험장 표로만 보이고 차고 정렬 화면은 M5 | §4.4 정렬 손상의 세부 | M3/M4 타이어 마모, M5 차고 |
| P27 | 내부 부품은 **포르쉐의 엔진·변속기 블록**뿐이다(마운트·스너버·파손·공동 충돌·구동 반력). 블록은 실제 유닛보다 작은 충돌 대리체(엔진 0.66×0.48×0.66 m — 뒤 서스펜션 피벗의 격자를 남기려고)이고, 벨하우징 사이의 격자 단면(뒤 크로스멤버)은 충돌 표면이 없다. 배터리·연료탱크·라디에이터는 여전히 격자 위치의 손상 그룹이다. 크랭크 회전 관성의 자이로 효과와 엔진 자체 토크 반력(블록 롤)은 없다(유닛 전체의 구동 반력만) | §4.4 | 다른 차 물리화(M3g) 때 같은 블록 방식, 필요하면 배터리·탱크 블록 |
| P22 | 경첩 패널(후드·도어): 래치가 풀린 앞 후드는 약 180 km/h까지 펄럭이고 280 km/h에서 뜯겨 나간다(M2g 실측). 패널 공력은 정지 공기 + 바람 기준이며 **다른 차의 후류는 반영하지 않는다**(외피 공력은 반영) | 드래프팅 중 패널 거동 | M3 |
| P23 | **외피 공력**(M2l)은 평판 뉴턴 모델(바람받이 압력 cos²·바람그늘 흡입·마찰)을 차의 Cd·A와 축 양력 합계로 보정한 것이다. 차체 아래 흐름·박리·지면 효과는 잔여 양력 상수로만 들어간다(자세에 따라 변하지 않음). 슬립스트림은 젠슨 후류(상수 a 0.25, k 0.1)로, CFD가 아니다. 돌풍·측풍 변화([선택])는 없고 일정한 바람만 있다. 리어 스포일러의 **공력 요소는 섀시 격자에 달려 있고**, 모델의 스포일러 메시는 움직이지 않는다 | 공력 정밀도·시각 | M8 레이스카(§14.1)에서 측정값으로 재보정 |
| P24 | 타이어 탈락 뒤 떨어진 카커스는 모델 메시가 없어 **노드 크기 구**로 그린다(LooseParts). 타이어 메시의 평평해짐은 정점 단계에서 노면 선 아래를 자르고 옆으로 부풀리는 근사다 | 파손 연출 | M4 |
| P25 | 정적 BVH(M2m)는 정적 형상을 추가한 뒤 **다음 스텝에 한 번 전체를 다시 짓는다**(O(n log n)). 스트리밍으로 셀을 수시로 넣고 빼는 대형 맵에는 셀 단위 BVH가 필요하다 | M6 대형 맵 | M6: 셀 BVH + 스트리머(A§4.6) |
| P28 | **충돌 반발이 실제보다 크다**: 56 km/h 강체벽 반발 911 약 11 km/h(19 %), 고스트 약 12 km/h(21 %), 마이바흐 약 15 km/h(27 %) — 실차 5~15 %. 격자가 명시 적분 한계로 실제 강판보다 무르고, 특히 **승객 셀**이 탄성 에너지를 품었다 돌려준다. 가파른 제하(unloadRatio α = 5)는 항복 가까이 눌린 크럼플 존에만 걸어(셀에 걸면 셀이 눌린 채 굳음) 효과가 제한적이다 | 충돌 반발 | 격자 밀도·질량 재분배로 셀 강성을 높이거나 셀 전용 제하 모델 |
| P29 | 조향을 고정한 직진 가속에서 **느린 방향 표류**가 있다(250 km/h 부근 30 s에 약 0.07~0.09 rad, 911·고스트). 모델 좌우 비대칭(가져온 모델의 바퀴 위치가 좌우 수 cm 다름)과 토 컴플라이언스 때문으로 보이며, 운전자는 약한 쏠림으로 느낀다 | 직진성 | 휠 정렬 보정(정적 토 트림) |
| P30 | **손상 유지 반복 측면 기둥 충돌**: 이미 접힌 도어 부위로 기둥을 거듭 들이받으면(2·3번째) 수지 오차가 6~37 kJ로 커진다(정면 벽 반복은 1 kJ 이내). 접힌 틈에 끼인 노드의 CCD 순환(P6·P21)으로 보인다. 겉보기 거동은 정상(튀어 오르지 않음) | 반복 충돌 수지 | P6 접촉 시점 보간과 함께 |

## 맵·월드 (§13)

| # | 내용 | 영향 | 계획 |
|---|---|---|---|
| M1 | 오픈월드는 **한빛시 1개(36 km²)**와 테스트 그라운드 **종합 주행시험장 1개(25 km²)**, 그리고 기존 무한 그리드 주행장뿐이다. §13.2의 지방·미국 서부·알프스·항만·대자연·모터스포츠 파크 맵과 §13.3의 크래시 테스트 센터(맵 형태)·오프로드·윈터·스턴트·도시 인프라·드리프트 패드 맵은 아직 없다. 대표 맵 100 km² 목표도 미달 | 맵 다양성 | 같은 생성기(`web/src/world/builder.ts`)로 맵 추가 |
| M2 | 지형은 **DEM이 아니라 절차 생성**(노이즈 + 규칙)이며 침식 시뮬레이션이 없다. 해상도 한빛시 3 m, 시험장 4 m 격자. 원경 산맥은 시각용 링 메시(충돌 없음) | 지형 사실감 | 침식 패스, 실제 DEM 가져오기 |
| M3 | 도로 곡선은 제어점을 지나는 Catmull–Rom 스플라인이라 **완화곡선(클로소이드)을 정식으로 설계하지 않는다**(곡률은 연속, 편경사는 곡률에서 계산 후 런오프로 평활). 종단곡선도 이동평균 + 경사 제한으로 만든다 | 도로 설계 정밀도 | 수평 선형을 직선–클로소이드–원곡선으로 |
| M4 | **AI 교통이 없다**(차량은 플레이어 1대). 신호등은 주기대로 바뀌기만 한다. 주차 차량 6대는 **차 크기의 강철 격자**(1.35 t, 찌그러지고 밀림)에 단순 차체 모양을 씌운 것으로 완전한 차량이 아니다 | 살아 있는 세계 | 차량 물리 LOD와 함께 AI 교통 |
| M5 | **물 물리가 없다**. 강·호수는 수면만 그려지고 차는 바닥(강바닥)까지 가라앉는다. 도하 시험로(§13.3)도 없음 | 수상·도하 | 부력·항력 요소 |
| M6 | 가로수·가로등·신호등·표지판 기둥은 **충돌이 없다**(시각만). 숲 나무도 마찬가지. 건물·교각·터널·방음벽·가드레일·중앙분리대·연석은 충돌한다 | 사물 충돌 | 부러지는 기둥·나무(파손 물체) |
| M7 | 교차로는 **격자 도심의 평면 교차(포장 박스 + 신호등)와 T자 합류**만 자동 생성한다. 회전교차로 1곳, 다이아몬드 IC 1곳(램프 4개, 병렬 합류)은 배치로 만들었다. 지하차도는 1곳(누리지하차도: 중앙대로 밑 92 m 박스, 양쪽 개착 경사면 — 실제의 수직 옹벽이 아니라 0.65 경사 절토면)이다. 입체 JCT, 도심 고가차도, 지하주차장 나선 램프, 휴게소는 없다. 교차로 모서리에 곡선 연석(귀퉁이 곡률)이 없다 | 도로망 다양성 | 교차로 생성기 확장 |
| M8 | 날씨는 노면 재질을 **통째로 바꾼다**(비: 아스팔트→젖은 아스팔트 등, 눈 쌓임: 압설). 물웅덩이·수막 두께·결빙 구간 같은 국부 상태는 없다. 바람은 물리(공력)와 나무에 전달된다 | 날씨 노면 사실감 | M3d 수막·타이어 온도와 함께 |
| M9 | 달의 위치는 태양 반대편에 날짜로 위상만 흉내 낸 근사다. 별은 무작위 배치 | 야간 하늘 | 천체력 |
| M10 | 한빛시 생성은 데스크톱 약 3초(시험장 1.5초, 맵 생성 워커), 휴대폰은 더 오래 걸린다. 한빛시 물리 삼각형 약 20만 개 + 높이장 400만 표본(약 16 MB)을 물리 워커로 복사한다. 재시작·텔레포트는 저장된 맵에서 물리 월드를 다시 짓는다(텔레포트 시 차량 손상도 초기화) | 로딩·메모리 | 타일 스트리밍 |
| M11 | 도로 위 소품(맨홀·포트홀·빗물받이·철도 건널목 등)은 시험장 방지턱 시험로에만 일부 있다. 도시 도로에는 아파트 단지 진입로의 원형 방지턱(길이 3.6 m, 높이 8 cm, 58 m 간격)만 있다 | 노면 디테일 | M3e 도로 시설 프리팹 |
| M12 | 건물은 절차 생성 상자 조합(포디움·타워·셋백·옥상 설비·박공지붕)이고 창은 셰이더 무늬다. 발코니 형상·간판 글자·아파트 동 번호 같은 세부는 없다. 3D 맵 선택의 미니어처는 도로를 지형에 그린 그림이다(교량·고가만 입체) | 도시 사실감 | 건물 파사드 모듈·텍스처 |
| M13 | 3D 전체 지도는 실제 월드를 위에서 보는 방식이라 휴대폰에서는 맵 전체(지형·건물·도로)를 한 번에 그려 프레임이 떨어질 수 있다(열려 있는 동안 물리는 멈춤). 나무·가로등은 일정 거리 안만 그린다 | 모바일 성능 | 원거리 대리 메시 |

## 웹 클라이언트

| # | 내용 | 영향 | 계획 |
|---|---|---|---|
| W1 | 이 개발 컨테이너의 헤드리스 Chromium 141(SwiftShader)에서는 **WebGPU 캔버스 디바이스가 1초 안에 유실된다**. three.js 없이 순수 WebGPU clear 루프만으로도 재현된다. 그래서 E2E와 스크린샷은 three의 **WebGL2 백엔드**(`?backend=webgl2`)로 찍었다. 물리·워커·WASM은 백엔드와 무관하게 같다 | 헤드리스 WebGPU 검증 불가 | 실제 GPU 브라우저에서 확인 필요(사용자 PC 벤치) |
| W2 | three r186은 텍스처 뷰에 항상 `swizzle: 'rgba'`(항등)를 넘기는데, Chromium 141 이하는 이를 거부해 매 프레임이 실패한다. 항등 swizzle만 제거하는 호환 심(`web/src/render/webgpuCompat.ts`)을 넣었다(렌더 결과는 같다) | 구형 브라우저 호환 | three·브라우저가 갱신되면 제거 |
| W3 | WebGPU 디바이스가 유실되면 WebGL2 백엔드로 **페이지를 다시 연다**(시뮬레이션 상태는 사라진다) | 드문 GPU 리셋 시 상태 손실 | M5: 상태 스냅샷 복원 |
| W4 | 렌더링 성능은 GPU 없는 컨테이너에서 측정할 수 없다. `bench/results/M0-web-headless-swiftshader.json`의 프레임 시간(0.4 fps)은 CPU 래스터라이저 수치라 **의미가 없다**. 물리 수치만 유효하다 | §21.7 렌더 목표 미검증 | 사용자 PC에서 `?bench=1` 실행 필요 |
| W5 | §18 셸(상단 바·단일 시트·일시정지·설정·메인 메뉴·맵 선택·로딩 화면·미니맵·월드맵)은 들어갔지만 게임패드 포커스 이동, HUD 편집 모드(드래그·크기), 색각 모드·고대비, 리플레이·포토 모드·텔레메트리 대시보드·튜닝 화면은 아직 없다. 라이트 테마 없음 | UX | M5 (§18) |
| W6 | 실시간 입력 링(SPSC)은 아직 없다. M1e 차량 입력은 렌더 프레임마다 postMessage로 보낸다(지연 최대 1프레임 + 워커 스텝 경계). 리플레이 결정론은 입력이 워커에 도착한 스텝 기준이다 | 입력 지연·리플레이 | M5: SAB 입력 링 + 스텝 번호 타임스탬프 |
| W7 | 번들의 three.js 청크가 약 940 kB(gzip 260 kB)라 Vite 경고가 나온다 | 초기 로딩 | M5에서 코드 분할 |
| W8 | 교차 출처 격리(COOP/COEP)가 없는 호스트(Claude 아티팩트, 일반 정적 호스팅)에서는 SharedArrayBuffer를 쓸 수 없어 **단일 스레드 WASM + 메시지 전송**으로 돈다. 포르쉐 1대 주행은 실시간(스텝 약 0.33 ms, 예산 0.5 ms)이지만 무거운 장면(큐브 더미 등)은 시간 감속된다. 아티팩트 호스트는 .glb를 제공하지 않아 모델을 base64 텍스트로 싣고(약 33% 커짐), 번들 글꼴 대신 Google Fonts를 쓴다 | 성능·로딩 | 격리된 호스팅(`dist/_headers`)이 기본 배포 경로 |
| W9 | 주행·충돌 시험 차량은 **포르쉐 911 터보 하나**다(충돌 시험장의 차량 B도 같은 차). 롤스로이스 고스트·마이바흐 GLS는 시각 모델만 있다(물리 차량 JSON 없음). M1 보고서에서 M2로 잡았으나 M2 범위(PDF §22)가 아니어서 M3로 옮긴다 | 차량 선택 | M3: 생성기로 두 차의 물리 차량(세단·SUV 격자·서스펜션) |
| W10 | 헤드리스 E2E는 SwiftShader라 초당 약 1프레임이다. 충돌 시험장의 에너지·운동량 그래프는 렌더 프레임마다 한 표본이라 헤드리스에서는 표본이 적다(실제 GPU에서는 60 fps) | E2E 그래프 해상도 | 없음(측정 환경 한계) |
| W11 | 도구(§20): 크레인 갈고리는 노드 위 4 m에 고정이다(크레인 이동·붐 없음). 견인 로프(노드↔노드)는 코어 API만 있고 UI가 없다. 노드 잡기의 힘은 잡은 바디 무게의 2배까지(차 25 kN 이상은 들지 못함) | 샌드박스 도구 | M3 셋업·차량 에디터와 함께 |
| W12 | 헤드리스 Chromium에서 `GL_INVALID_OPERATION: glDrawElements: Must have element array buffer bound` 경고가 한 번 나온다(M1부터, 렌더 결과 영향 없음) | 콘솔 경고 | 원인 추적(three WebGL2 백엔드) |

## 프로젝트·에셋

| # | 내용 |
|---|---|
| A1 | 새 저장소를 이 세션에서 만들 수 없었다(GitHub 통합 권한 403). 그래서 `Runner` 저장소의 `apex-sim/` 폴더에서 개발 중이다. 빈 저장소가 생기면 히스토리를 보존해 옮긴다(`git subtree split --prefix apex-sim`) |
| A2 | 사용자 지정 예외: 포르쉐 911 터보, 롤스로이스 고스트, 마이바흐 GLS는 사용자가 이전에 만든 모델(실존 차량 디자인)을 다듬어 쓴다(§1.7 오리지널 IP 원칙의 예외, 사용자 승인). 그중 911과 GLS의 **원본 출처·라이선스는 기록이 없다**(과거 저장소에는 "사용자 제공"으로만 남아 있다). 공개 배포 전에 확인이 필요하다 |
| A4 | 가져온 사용자 모델의 흠과 보정: 마이바흐 GLS 베이크에는 앞유리·파노라마 루프 유리·뒷유리가 없고 그릴·헤드램프 뒤가 비어 있다. 헤드램프·테일램프 렌즈도 빠져 있었다. 임포터가 이 자리를 **합성한 면**(주변 프레임을 잇는 막면, 어두운 백킹, 3 cm 격자라 램프 가장자리가 계단 모양)으로 채운다. 스캔 데이터가 아니므로 곡률은 근사다. 휠은 두 차 모두 베이크 메시 대신 절차 휠을 아치 실측으로 맞춘다(마이바흐 285/40 R23, 휠베이스 3.144 m / 실차 3.135 m, 고스트 255/45 R20·285/40 R20). 고스트 보닛은 정점 색으로 어둡다(M4 카 페인트 셰이더에서 도색 처리). 포르쉐 램프 정점색은 베이크 그대로(베이지)다. |
| A3 | 네트워크 정책으로 Poly Haven·ambientCG에 접근할 수 없다(KICKOFF Q5). 에셋은 절차 생성 + `TEMP_` 슬롯으로 진행한다 |
