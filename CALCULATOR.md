# NEXUS CALC — 돌리고 부수는 3D 계산기

`calculator.html` 한 장으로 끝나는 자립형 페이지입니다. 라이브러리 없이 직접
쓴 WebGL 렌더러로 돌아가며, 웹폰트를 제외하면 어떤 네트워크 요청도 하지
않습니다. (이 저장소의 Vite 러너 게임과는 별개이며 `npm run build` 에 영향을
주지 않습니다.)

아티팩트: <https://claude.ai/artifact/MfAcXDBuquRFApi9N5DoaZ>

## 진짜 3D 모형

기기 전체가 28개의 박스 프리미티브로 만들어져 있습니다. CSS 트랜스폼이 아니라
원근 투영·법선 조명·베벨 셰이딩이 들어간 실제 3D 지오메트리입니다.

- **드래그로 궤도 회전**, 휠·핀치로 줌, 놓으면 관성으로 더 돌다가 멈춥니다.
  4초간 가만히 두면 천천히 자동 회전합니다.
- **키를 직접 누릅니다.** 화면 좌표에서 쏜 광선을 각 박스의 OBB와 교차시켜
  어떤 키캡을 눌렀는지 찾고, 그 키가 눌려 들어갑니다. 10px 이상 끌면 회전으로
  간주해 키가 눌리지 않습니다.
- 키캡 각인은 1024×1024 아틀라스 한 장을 셀 단위로 샘플링하고, 디스플레이는
  2D 캔버스를 그대로 텍스처로 올립니다. 밝은 키캡에는 어두운 각인이 들어갑니다.
- 배경은 같은 카메라를 공유하는 프래그먼트 셰이더입니다 — 네온 그리드 바닥,
  별, 기기 아래로 떨어지는 소프트 섀도, 키 입력마다 퍼지는 충격파.

## 돌리면 다른 기기가 됩니다 (PRO)

세 형태가 **같은 박스 28개를 공유**합니다. 형태를 바꾸면 각 박스가 새 위치·
크기·회전으로 보간되어 흘러가고, 그 형태에 없는 박스는 0으로 줄어듭니다.

| 형태 | 모습 | 하는 일 |
| --- | --- | --- |
| 계산기 | 4×5 키패드 + LCD | 사칙연산, %, ±, 소수점 |
| 컴퓨터 | 모니터 + 스탠드 + 타워 + 키보드 | NEXUS LAB — 공학 계산 12종, DEG/RAD |
| 노트북 | 12° 젖혀진 화면 + 베이스 | 히스토리 테이프 탐색 · 값 불러오기 |

좌우로 빠르게 튕기거나 `↻ 뒤집기`, `F` 키로 다음 형태로 넘어갑니다.

## 부수기 & 커스터마이즈 (PRO)

- **부수기** — 28개 박스가 중심에서 바깥으로 튀어나가 중력·바닥 반발·각속도로
  굴러다닙니다. 흩어진 조각을 클릭하면 다시 걷어찹니다. `조립`을 누르면 원래
  자리로 스프링처럼 돌아옵니다. `R` 키, 더블클릭으로도 됩니다.
- **커스터마이즈** — 본체 8색, 키캡 8색, 악센트 6색, 마감 3종(매트/새틴/유광),
  프리셋 6종. 악센트는 배경 네온까지 함께 바뀝니다. 구성은 브라우저에
  저장됩니다.

## 페이월

1. **무료 체험은 평생 1회.** 처음 `=` 를 누르면 계산이 되고, 1.1초 뒤 결과가
   흐려지면서 결제창이 열립니다.
2. 이후 `=` 와 PRO 기능(형태 전환·부수기·커스텀)은 모두 결제창으로 연결됩니다.
3. 결제(데모)를 마치면 PRO가 켜지고 컴퓨터 형태로 전환되며 전부 열립니다.
4. 우측 상단 **라이선스 초기화** 로 처음 상태로 되돌립니다.

### 결제창

실제 체크아웃 구조 그대로입니다 — 좌측 주문 요약(정가·할인·부가세·합계), 우측
결제 폼(이메일, 카드 묶음 필드, 소유자명, 국가·우편번호), 진행 오버레이, 그리고
주문번호·카드 끝 4자리·일시가 찍힌 영수증. 검증도 실제와 같습니다: **Luhn
체크섬**, 만료일 과거 거부, 이메일 형식, CVC 자릿수.

상단에 **TEST MODE** 리본이 고정되어 있습니다. 실제 결제는 일어나지 않고, 입력한
카드 정보는 어디로도 전송되지 않으며(페이지가 보내는 요청은 웹폰트뿐입니다)
결제 완료 시점에 CVC는 즉시 비워집니다.

### Human verification

결제 버튼은 사람 확인을 통과해야 활성화됩니다. `저는 로봇이 아닙니다` 체크 →
3×3 타일 챌린지(제시된 글자가 보이는 타일 모두 선택, 정답 2~4개 무작위, 글자는
회전·기울임·투명도가 흔들린 채 스캔라인 위에 그려짐). 틀리면 흔들리며 새 문제가
나옵니다.

## 조작

드래그 회전 · 휠 줌 · 키 클릭 · `F` 뒤집기 · `R` 부수기/조립 · 더블클릭 부수기 ·
숫자와 `+ - * / . % ^` · `Enter`(=) · `Backspace`(⌫) · `Esc`·`C`(AC) · `x`(×) ·
`n`(±)

## 성능

프레임 타임 감시기가 느린 GPU에서 렌더 해상도를 2단계 낮춥니다.
`prefers-reduced-motion` 을 존중하고, WebGL을 못 쓰면 안내 문구로 대체합니다.

## 확인한 것

Chromium(Playwright), 1100×820 / 390×844: 3D 픽 판정으로 키 입력, 키보드 입력,
무료 1회 → 잠김 → 결제창, PRO 기능 잠금(부수기 클릭 시 결제창), 사람 확인 통과,
결제 → 영수증 → PRO → 컴퓨터 형태 자동 전환, 노트북 형태, 부수기/조립,
프리셋 적용과 저장, 새로고침 후 라이선스·테이프·테마 유지, 모바일 가로 오버플로
없음, 콘솔 에러 없음.
