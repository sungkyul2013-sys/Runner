# NEXUS CALC — 돌릴 수 있는 3D 계산기 (페이월 데모)

`calculator.html` 한 장으로 끝나는 자립형 페이지입니다. 빌드도, 의존성도,
네트워크 요청도 없습니다. 브라우저로 열면 바로 동작합니다. (이 저장소의 Vite
러너 게임과는 별개이며 `npm run build` 에 영향을 주지 않습니다.)

## 돌리고 만지는 3D 본체

- **드래그하면 돌아갑니다.** 본체 어디든 잡고 좌우로 끌면 Y축으로 회전하고,
  놓으면 관성으로 더 돌다가 가장 가까운 면으로 스냅됩니다.
- 10px 이상 끌면 그 아래 키는 눌리지 않습니다 — 돌리다가 숫자가 입력되는 일이
  없습니다.
- 커서를 따라 미세하게 기울고(±9° / ±11°), 가만히 두면 위아래로 떠 있습니다.
- `↻` 버튼 또는 **F** 키로 뒤집습니다.
- 앞뒤 면은 `backface-visibility`로 분리되어 있고, 두 면 사이는 16겹 z-레이어로
  실제 두께를 만들었습니다. 뒤를 보고 있을 땐 앞면의 버튼이 클릭·탭 포커스에서
  빠집니다.

## 앞면 — 계산기

사칙연산, `%`(앞 숫자를 1/100), 부호 반전 `±`, 소수점, `AC`, `⌫`.
파서는 직접 구현한 재귀 하강 방식이라 `eval`을 쓰지 않으며, 거듭제곱 `^`은
오른쪽 결합입니다. 키보드: 숫자, `+ - * / . % ^`, `Enter`(=), `Backspace`(⌫),
`Esc`·`C`(AC), `x`(×), `n`(±), `F`(뒤집기).

## 뒷면 — NEXUS LAB (PRO 전용)

- **공학 계산 12종**: √x, x², xʸ, 1/x, sin, cos, tan, π, log, ln, e, n!
- **DEG / RAD 토글** — 삼각함수에 바로 반영됩니다.
- **히스토리 테이프** — 계산 기록이 쌓이고, 한 줄을 누르면 그 값을 불러오며
  자동으로 앞면으로 돌아갑니다. `localStorage`에 최대 40줄 저장됩니다.
- PRO가 아니면 뒷면 전체가 잠금 베일로 덮이고, 거기서 바로 결제로 이어집니다.

## 페이월

1. **무료 체험은 평생 1회.** 처음 `=` 를 누르면 정상 계산 후 1.1초 뒤 결과가
   흐려지면서 결제창이 열립니다.
2. 이후 `=` 를 누를 때마다 결제창이 다시 열립니다. 숫자 입력·`AC`·`⌫`는 계속
   사용 가능합니다.
3. 결제(데모)를 마치면 PRO가 활성화되어 `=`가 무제한이 되고 **뒷면 LAB이
   열립니다**. 상태는 `nexuscalc.license.v1`에 저장됩니다.
4. 우측 하단 **라이선스 초기화 (데모)** 로 언제든 처음 상태로 되돌립니다.

### 결제창

실제 체크아웃 구조를 그대로 따릅니다 — 좌측 주문 요약(정가/할인/부가세/합계),
우측 결제 폼(이메일, 카드 정보 묶음 필드, 소유자 이름, 국가·우편번호),
결제 진행 오버레이, 그리고 주문번호·카드 끝 4자리·일시가 찍힌 영수증 화면.

검증도 실제와 같습니다: **Luhn 체크섬**, 만료일 과거 여부, 이메일 형식, CVC
자릿수. 통과하지 못하면 해당 필드가 빨갛게 표시되고 포커스가 이동합니다.

상단에는 **TEST MODE** 리본이 고정되어 있습니다. 실제 결제는 일어나지 않고,
입력한 카드 정보는 **어디로도 전송되지 않습니다** — 이 페이지는 네트워크 요청을
한 건도 보내지 않습니다. `테스트 카드 자동 입력` 링크로 테스트 번호
(4242 4242 4242 4242)를 한 번에 채울 수 있고, 결제 완료 시점에 CVC 입력값은
즉시 비워집니다.

### Human verification

결제 버튼은 사람 확인을 통과해야 활성화됩니다.

1. `저는 로봇이 아닙니다` 체크 → 확인 스피너
2. 3×3 타일 챌린지: 제시된 글자(`7`, `π`, `√`, `×`, `3`, `∞`, `∑` 중 하나)가
   보이는 타일을 모두 고릅니다. 정답 타일은 2~4개이며 매번 무작위로 생성되고,
   글자는 회전·기울임·투명도가 흔들린 상태로 스캔라인 위에 그려집니다.
3. 틀리면 흔들리며 새 문제가 나오고, 맞히면 체크가 초록으로 바뀌며 결제 버튼이
   열립니다. `⟳`로 새 문제를 받을 수 있습니다.

## 3D 배경

WebGL 프래그먼트 셰이더 레이마칭 — 회전하는 크리스털 코어, 네온 링 3개, 무한
그리드 바닥, 별. 경로 길이 기반 볼류메트릭 글로우와 휘도 기준 Reinhard
톤매핑을 씁니다. 키를 누를 때마다 바닥에 충격파가 퍼지고, 팔레트가 체험 중
시안 → 잠김 크림슨 → PRO 골드로 바뀝니다. 프레임 타임 감시기가 느린 GPU에서
해상도를 3단계 낮추고, 그래도 느리면 절반 프레임 → CSS 그라디언트로 물러납니다.
`prefers-reduced-motion`도 존중합니다.

## 확인한 것

Chromium(Playwright), 데스크톱 1180×900 / 모바일 390×844:

- 실제 마우스로 키 클릭, 드래그 회전 후 뒷면으로 스냅, 드래그가 키 입력을
  일으키지 않음
- 비PRO 상태에서 뒷면 잠금 베일 노출, `앞면으로` 복귀
- 무료 1회 계산 → 잠김 → 결제창, 결제 버튼이 사람 확인 전에는 비활성
- 오답 후 새 문제 생성, 정답 시 결제 버튼 활성화
- Luhn 실패 / 만료 카드 / 잘못된 이메일 각각의 오류 메시지
- 결제 → 영수증(주문번호·끝 4자리·일시) → PRO 활성화 → 자동으로 LAB 표시
- LAB: √81=9, 제곱, 81!, π 삽입, DEG↔RAD, sin(90°)=1, 2^10=1024, 테이프 누적
- 새로고침 후 라이선스·테이프 유지, 초기화 동작
- 모바일 가로 오버플로 없음, 챌린지가 뷰포트 안에 들어옴, 콘솔 에러 없음
