# NEXUS CALC — 3D 계산기 (페이월 데모)

`calculator.html` 한 장으로 끝나는 자립형(standalone) 페이지입니다. 빌드도,
의존성도, 네트워크 요청도 없습니다. 파일을 더블클릭해서 브라우저로 열면 바로
동작합니다. (이 저장소의 Vite 러너 게임과는 별개이며 `npm run build` 에 영향을
주지 않습니다.)

## 동작

1. **무료 체험은 평생 1회.** 처음 `=` 를 누르면 정상적으로 계산해 결과를
   보여준 뒤, 1.1초 후 결과가 흐려지면서 결제 화면이 올라옵니다.
2. **그 뒤로는 잠김.** `=` 를 누를 때마다 결제 화면이 다시 열립니다. 숫자 입력과
   `AC` / `⌫` 는 계속 쓸 수 있습니다.
3. **$1,299.00 결제(데모)** 를 마치면 PRO 라이선스가 활성화되고 `=` 가 무제한이
   됩니다. 상태는 `localStorage` 의 `nexuscalc.license.v1` 에만 저장됩니다.
4. 우측 하단 **라이선스 초기화 (데모)** 로 언제든 처음 상태로 되돌립니다.

## 결제 화면은 데모입니다

실제 결제는 일어나지 않습니다. 카드 입력값은 **어디로도 전송되지 않고**
저장되지도 않습니다 — 페이지는 네트워크 요청을 단 한 건도 보내지 않습니다.
카드번호는 테스트용 `4242 4242 4242 4242` 로 미리 채워져 있고, 화면 상단에는
데모 안내 배너가 항상 붙어 있습니다. 결제 진행 표시는 순수한 연출이며,
`완료` 시점에 CVC 입력값은 즉시 비워집니다.

## 3D 이펙트

- **배경**: WebGL 프래그먼트 셰이더 레이마칭. 회전하는 크리스털 코어 + 네온 링
  3개 + 무한 그리드 바닥 + 별, 경로 길이 기반 볼류메트릭 글로우, 휘도 기준
  Reinhard 톤매핑. 키를 누를 때마다 바닥에 충격파가 퍼집니다.
- **본체**: CSS `preserve-3d`. 16겹 z-레이어로 두께를 만든 바디, 캡과 샤프트가
  분리된 키, 커서를 따라가는 기울임(±20° / ±13°)과 상시 부유 애니메이션,
  흐르는 스페큘러 하이라이트.
- **상태에 따른 색**: 체험 중에는 시안, 잠기면 크림슨, PRO가 되면 골드로 셰이더
  팔레트와 UI가 함께 바뀝니다.
- **결제 시트**: 3D 카드(CVC 입력 시 뒤집힘), 회전하는 컨픽 광택, 성공 시
  금색 씰과 컨페티 버스트.
- **성능**: 프레임 타임 감시기가 느린 GPU에서 해상도를 3단계로 낮추고, 그래도
  느리면 배경을 절반 프레임으로, 마지막에는 CSS 그라디언트로 대체합니다.
  `prefers-reduced-motion` 도 존중합니다.

## 계산기

- 사칙연산, 백분율(`%` 는 앞 숫자를 1/100로), 부호 반전 `±`, 소수점, `AC`, `⌫`.
- 파서는 직접 구현한 재귀 하강 방식입니다 — `eval` 을 쓰지 않습니다.
- 0으로 나누기 / 범위 초과 / 잘못된 식은 한국어 오류 메시지로 표시됩니다.
- 키보드: 숫자, `+ - * / . %`, `Enter`(=), `Backspace`(⌫), `Esc`·`C`(AC),
  `x`(×), `n`(±).

## 확인한 것

Chromium(Playwright)에서 데스크톱 900×940 / 모바일 390×844 기준으로 확인:
무료 1회 → 잠김 → 결제 → PRO 흐름, 결제 화면 닫았다 `=` 로 재진입, 폼 검증,
새로고침 후 라이선스 유지, 초기화, 계산 정확성(사칙연산·%·±·0 나누기·체이닝),
키보드 입력, 두 스텝 모두 뷰포트 안에 들어오는지, 콘솔 에러 없음.
